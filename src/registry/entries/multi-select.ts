import { MultiSelectPreview } from "@/registry/previews/multi-select-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the multi-select component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`, so adding a component never means editing a
 * file that another author may be editing at the same time.
 */
export const entry: RegistryEntry = {
  slug: "multi-select",
  name: "Multi Select",
  nameEs: "Selección múltiple",
  category: "inputs",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A combobox for picking several values at once: each choice becomes a removable chip, and a filterable listbox with a check mark on selected options handles the rest. Built from the ARIA combobox pattern because no native control shows chips, with the same material recipes, tactile focus well and three sizes as the rest of the inputs.",
  descriptionEs:
    "Un combobox para elegir varios valores a la vez: cada opción se convierte en un chip que se puede quitar, y una lista filtrable con una marca de verificación en las opciones seleccionadas hace el resto. Construido con el patrón ARIA de combobox porque ningún control nativo muestra chips, con las mismas recetas de material, el mismo pozo de foco táctil y los tres tamaños que el resto de los campos.",
  variants: [{ id: "default", label: "Default", labelEs: "Por defecto" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "There is no native control for several values shown as removable chips, so this is the one input assembled from ARIA rather than inherited from the platform: a role=combobox text input with aria-expanded, aria-controls and aria-haspopup=listbox drives a role=listbox with aria-multiselectable=true whose role=option items each carry aria-selected. Focus stays on the input throughout; the active option is tracked with aria-activedescendant, so ArrowDown and ArrowUp move a highlight rather than the focus ring, exactly as the combobox pattern prescribes, with Home and End jumping to the first and last enabled option and disabled options skipped. Enter toggles the active option — its aria-selected flips and a chip is added or removed — Backspace on an empty input removes the last chip, and Escape closes the list. Every chip's remove control is a real button with an aria-label of the form Remove <label>, reachable and operable by keyboard, and options are also togglable by pointer. Selection is never signalled by colour alone: a selected option carries aria-selected, a check glyph (a shape, not a hue) and a heavier font weight, and a visually hidden aria-live=polite region announces each add and remove with a running count so a screen-reader user hears what changed. Filtering is a deterministic case-insensitive substring match on the label, with no Math.random or Date.now in render and all ids from React.useId, so server and client output agree. The single source of truth for the error state is aria-invalid on the combobox input; the control row mirrors it through a data attribute so what is shown and what assistive tech is told cannot drift. MultiSelectField renders a real <label htmlFor> bound to the input and composes aria-describedby rather than overwriting it, and its message region is always mounted, even when empty, because an aria-live region must exist before its text arrives for the announcement to be reliable. The focus affordance is a 2px offset outline drawn around the whole control on has-[:focus-visible] plus a material-specific inset well that presses in on focus-within; the outline is the part that never animates and never disappears, while the well is decorative reinforcement. prefers-reduced-motion removes the dropdown's slide and the interpolation but keeps every final state — the panel still opens, the chip still appears, the option still highlights. forced-colors clears the gradients and tactile shadows, keeps the control and chip bounds with CanvasText borders, marks the active option with Highlight and HighlightText, keeps the invalid border as Mark, and colours the informative glyphs with system colours, and the opaque popup surface means the list is readable over any backdrop, including the translucent glass recipe. Contrast: typed text, placeholder, chip text and option text all measure at or above 4.5:1 against the surface they sit on for every material, and the check glyph is held to that same 4.5:1 rather than the 3:1 WCAG 1.4.11 would allow an informative graphic.",
  a11yEs:
    "No existe un control nativo para varios valores mostrados como chips que se pueden quitar, así que este es el único campo del lote ensamblado con ARIA en vez de heredado de la plataforma: un input de texto con role=combobox, aria-expanded, aria-controls y aria-haspopup=listbox gobierna un role=listbox con aria-multiselectable=true cuyas opciones role=option llevan cada una aria-selected. El foco permanece en el input todo el tiempo; la opción activa se sigue con aria-activedescendant, de modo que ArrowDown y ArrowUp mueven un resaltado y no el anillo de foco, tal como prescribe el patrón de combobox, con Home y End saltando a la primera y última opción habilitada y las opciones deshabilitadas omitidas. Enter alterna la opción activa —su aria-selected cambia y se añade o quita un chip—, Backspace con el input vacío elimina el último chip y Escape cierra la lista. El control para quitar de cada chip es un botón real con un aria-label de la forma Remove <etiqueta>, accesible y operable por teclado, y las opciones también se alternan con el puntero. La selección nunca se transmite solo con color: una opción seleccionada lleva aria-selected, un glifo de verificación (una forma, no un tono) y un peso de fuente mayor, y una región aria-live=polite oculta a la vista anuncia cada alta y baja con un recuento acumulado para que quien use lector de pantalla escuche qué cambió. El filtrado es una coincidencia de subcadena sin distinción de mayúsculas sobre la etiqueta, sin Math.random ni Date.now en el render y con todos los ids de React.useId, así que la salida de servidor y cliente coinciden. La única fuente de verdad del estado de error es aria-invalid en el input del combobox; la fila del control lo refleja mediante un atributo de datos para que lo que se ve y lo que se le dice a la tecnología de asistencia no se desincronicen. MultiSelectField renderiza un <label htmlFor> real asociado al input y compone aria-describedby en vez de sobrescribirlo, y su región de mensaje está siempre montada, incluso vacía, porque una región aria-live debe existir antes de que llegue su texto para que el anuncio sea fiable. La señal de foco es un contorno de 2px con desplazamiento dibujado alrededor de todo el control en has-[:focus-visible] más un pozo interior propio del material que se hunde en focus-within; el contorno es la parte que nunca se anima ni desaparece, mientras que el pozo es refuerzo decorativo. prefers-reduced-motion quita el deslizamiento del desplegable y la interpolación pero conserva cada estado final: el panel sigue abriéndose, el chip sigue apareciendo, la opción sigue resaltándose. forced-colors limpia los degradados y las sombras táctiles, mantiene los límites del control y de los chips con bordes CanvasText, marca la opción activa con Highlight y HighlightText, conserva el borde inválido como Mark y colorea los glifos informativos con colores del sistema, y la superficie opaca del desplegable hace que la lista sea legible sobre cualquier fondo, incluida la receta translúcida de vidrio. Contraste: el texto escrito, el placeholder, el texto de los chips y el de las opciones miden todos 4,5:1 o más contra la superficie sobre la que se apoyan en cada material, y el glifo de verificación se somete a ese mismo 4,5:1 en vez del 3:1 que la 1.4.11 de WCAG permitiría a un gráfico informativo.",
  sourcePath: "src/registry/ui/multi-select.tsx",
  Preview: MultiSelectPreview,
};
