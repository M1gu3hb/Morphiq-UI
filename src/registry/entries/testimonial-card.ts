import { TestimonialCardPreview } from "@/registry/previews/testimonial-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the single testimonial card.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "testimonial-card",
  name: "Testimonial Card",
  nameEs: "Tarjeta de testimonio",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A single testimonial built as a figure wrapping a blockquote and a figcaption: a pull quote, a fixed-size avatar well, the author's name and role, a star rating whose value is real text, and an optional Helpful vote that stays clickable above the whole-card link.",
  descriptionEs:
    "Un testimonio individual construido como un figure que envuelve un blockquote y un figcaption: una cita destacada, un pozo de avatar de tamaño fijo, el nombre y el cargo de la persona, una valoración de estrellas cuyo valor es texto real y un voto opcional de Útil que sigue siendo clicable por encima del enlace de toda la tarjeta.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Semantics: the card is a <figure> wrapping a <blockquote> for the quotation and a <figcaption> for the attribution — the HTML pairing for a quote and its source, which is why the attribution sits outside the blockquote rather than inside it. Every card also contributes a real heading whose rank is set by headingLevel (never hardcoded to h3) because the correct level depends on the surrounding document outline; when no visible headline is supplied that heading renders screen-reader-only as \"Testimonial from <name>\" so the card is still reachable by heading navigation. Whole-card link: when href is set the card becomes ONE link via the stretched-link pattern — the anchor lives inside the heading when there is a headline, otherwise it is the visible \"Read the full story\" link, and in both cases a transparent ::after overlay covers the card. No <a> or <button> is ever nested inside another: the Helpful toggle sits at relative z-10 above that overlay so it stays independently clickable and focusable, and Enter/Space work natively on both controls. The linked card draws the focus ring on :focus-within as well as :focus-visible (the stretched anchor's own box is invisible, so the card is what shows its focus); an inert card skips :focus-within to avoid double-ringing the button's own focus. Image: the avatar sits in a fixed-size square well with intrinsic width and height, so nothing shifts while it loads, and it defaults to alt=\"\" because the author's name is announced immediately beside it — a descriptive alt would name the same person twice; pass avatarAlt when the portrait carries information the caption does not. With no image the well shows deterministic initials, hidden from assistive tech for the same reason. Rating: the value is real visible text (\"4.5 out of 5\") and the stars are aria-hidden decoration clipped to the exact fractional fill, so the rating is never carried by star-count or gold colour alone. The Helpful vote carries its state in aria-pressed AND in a changed label (\"Helpful\" to \"Marked helpful\") AND in a filled icon, never colour alone; its count is real text, and every toggle is announced through an aria-live=\"polite\" region that is present in the DOM, empty, from the first render. Dates arrive as props and render as <time dateTime>, formatted by a pure timezone-stable helper — nothing calls Date.now() during render, so a statically generated page cannot hydrate to a different day. Motion: the entrance rise, the hover lift, the arrow nudge and the toggle's icon pop are all cancelled under prefers-reduced-motion, and because neither keyframe declares a fill mode the card rests at its final visual state (opacity 1, no offset) with animations off. Forced colors: a system-coloured border keeps the card's bounds once shadows and translucency are discarded, the skeuo gradient and the glass backdrop blur are cleared by hand, glyphs repaint to CanvasText, the toggle falls back to ButtonFace/ButtonText and repaints to Highlight/HighlightText when pressed, and the focus ring becomes Highlight. Contrast: on every material the quote, name, role, date and rating value measure at or above 4.5:1 against the surface, the glass tokens carry their own tint so that holds over a white and a black backdrop alike, and each star colour clears 4.5:1 too even though the stars are decorative.",
  a11yEs:
    "Semántica: la tarjeta es un <figure> que envuelve un <blockquote> para la cita y un <figcaption> para la atribución — el emparejamiento HTML correcto para una cita y su fuente, por eso la atribución vive fuera del blockquote y no dentro. Cada tarjeta aporta además un encabezado real cuyo rango define headingLevel (nunca fijo en h3), porque el nivel correcto depende del esquema del documento; cuando no se pasa un titular visible, ese encabezado se renderiza solo para lectores de pantalla como \"Testimonial from <nombre>\", de modo que la tarjeta sigue siendo alcanzable navegando por encabezados. Enlace de toda la tarjeta: cuando se define href, la tarjeta se convierte en UN enlace mediante el patrón de enlace extendido — el ancla vive dentro del encabezado si hay titular y, si no, es el enlace visible \"Read the full story\"; en ambos casos un ::after transparente cubre la tarjeta. Nunca se anida un <a> o un <button> dentro de otro: el botón de Útil se eleva con relative z-10 por encima de esa capa para seguir siendo clicable y enfocable de forma independiente, y Enter/Espacio funcionan de forma nativa en ambos controles. La tarjeta enlazada dibuja el anillo de foco tanto en :focus-within como en :focus-visible (la caja del ancla extendida es invisible, así que es la tarjeta la que muestra el foco); una tarjeta inerte omite :focus-within para no duplicar el anillo del botón. Imagen: el avatar vive en un pozo cuadrado de tamaño fijo con ancho y alto intrínsecos, así que nada se desplaza mientras carga, y usa alt=\"\" por defecto porque el nombre de la persona se anuncia justo al lado — un alt descriptivo nombraría dos veces a la misma persona; pasa avatarAlt cuando el retrato aporte información que la leyenda no da. Sin imagen, el pozo muestra iniciales deterministas, ocultas a la tecnología asistiva por la misma razón. Valoración: el valor es texto visible real (\"4.5 out of 5\") y las estrellas son decoración aria-hidden recortada a la fracción exacta de relleno, así que la valoración nunca depende del número de estrellas ni del color dorado. El voto de Útil lleva su estado en aria-pressed Y en una etiqueta que cambia (\"Helpful\" a \"Marked helpful\") Y en un icono relleno, nunca solo en el color; su conteo es texto real y cada cambio se anuncia mediante una región aria-live=\"polite\" presente en el DOM, vacía, desde el primer renderizado. Las fechas llegan como props y se renderizan como <time dateTime>, formateadas con una función pura y estable frente a zonas horarias — nada llama a Date.now() durante el render, así que una página generada estáticamente no puede hidratar con otro día. Movimiento: la entrada, el levantamiento al pasar el ratón, el empuje de la flecha y el rebote del icono se cancelan bajo prefers-reduced-motion y, como ningún keyframe declara fill-mode, la tarjeta descansa en su estado visual final (opacidad 1, sin desplazamiento) con las animaciones apagadas. Forced-colors: un borde con color de sistema mantiene los límites cuando se descartan sombras y translucidez, el degradado de skeuo y el desenfoque del vidrio se limpian a mano, los glifos se repintan a CanvasText, el botón recurre a ButtonFace/ButtonText y se repinta a Highlight/HighlightText al estar presionado, y el anillo de foco pasa a Highlight. Contraste: en cada material la cita, el nombre, el cargo, la fecha y el valor de la valoración miden 4,5:1 o más contra la superficie, los tokens de vidrio llevan su propio tinte para que eso se cumpla igual sobre fondo blanco que negro, y cada color de estrella también supera 4,5:1 aunque las estrellas sean decorativas.",
  sourcePath: "src/registry/ui/testimonial-card.tsx",
  Preview: TestimonialCardPreview,
};
