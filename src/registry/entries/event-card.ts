import { EventCardPreview } from "@/registry/previews/event-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the event card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "event-card",
  name: "Event Card",
  nameEs: "Tarjeta de evento",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "An event listing with a torn-stub date plaque, a semantic heading, venue and time as real text, an optional poster image, and an Attend control that toggles to a confirmed Attending state — with a stretched-link pattern so the whole surface can lead to the event without trapping the button.",
  descriptionEs:
    "Una ficha de evento con una placa de fecha tipo talón, encabezado semántico, lugar y hora como texto real, imagen opcional de cartel y un control de Asistir que cambia a un estado confirmado de Asistiendo — con un patrón de enlace extendido para que toda la superficie lleve al evento sin atrapar el botón.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Each card is an <article> whose title is a real heading; the rank is overridable via headingLevel because the correct level depends on the surrounding document outline. The date is a genuine <time dateTime> element: its big day and short month are real text (selectable, zoomable, translatable) but are aria-hidden, because \"14 Mar\" read aloud is an abbreviation rather than a date — the element instead exposes the full spoken form (\"Saturday, 14 March 2026\") from the dateLabel prop, and its width is fixed so the row never reflows. Time and location are real text too, each preceded by a visually hidden \"Time:\" / \"Location:\" label with its icon hidden, so no meaning rests on a glyph. The optional poster image is a real <img> with author-supplied alt inside a fixed aspect-ratio well, so the box is laid out before the bytes arrive and nothing shifts on load. When href is set the whole card becomes ONE link via the stretched-link pattern — a single title <a> whose ::after overlay covers the card — and the Attend control is raised on relative z-10 so it stays independently clickable and focusable; no <a> or <button> is ever nested inside another, and Enter/Space work natively on both. Interactive cards draw the focus ring on :focus-within as well as :focus-visible so tabbing to the link or the button outlines the whole card; inert cards skip that to avoid double-ringing the button. The Attend control is a real <button> carrying aria-pressed, a LABEL that changes with the state (\"Attend\" → \"Attending\"), an icon that changes shape (plus calendar → check calendar) and a visually hidden copy of the event title so one card's control is distinguishable from the next; the outlined confirmed fill is a third, redundant cue and never the only one. The attendee count is real text (\"27 going\"), derived so the reader is counted in only while attending, and every toggle is announced through an aria-live=\"polite\" region that is present in the DOM before any text arrives. There is no drag interaction anywhere, so nothing is pointer-only. Under prefers-reduced-motion the card lift, the stub lift, the poster zoom and the stamp travel are all dropped while the end state is kept — the check glyph stays fully rendered and the confirmation ring is removed outright. In forced-colors mode a system-coloured border keeps the card's bounds once shadows and translucency are discarded, the glass backdrop filter and the skeuo gradient are cleared, the decorative perforation drops its background image, glyphs repaint to CanvasText, the pressed Attend control repaints to Highlight/HighlightText and the focus ring to Highlight. Contrast: on every material the title, the date plaque text and the muted meta rows measure at or above 4.5:1 against their own surface, and the glass recipe carries its own tint so that holds over a white and a black backdrop alike. Dates never come from the clock — every date and time string is a prop, so a statically generated page hydrates without mismatch.",
  a11yEs:
    "Cada tarjeta es un <article> cuyo título es un encabezado real; el rango es configurable con headingLevel porque el nivel correcto depende del esquema del documento. La fecha es un elemento <time dateTime> auténtico: el día grande y el mes abreviado son texto real (seleccionable, ampliable y traducible) pero llevan aria-hidden, porque \"14 Mar\" leído en voz alta es una abreviatura y no una fecha — el elemento expone en su lugar la forma completa (\"Saturday, 14 March 2026\") desde la prop dateLabel, y su ancho es fijo para que la fila nunca se recomponga. La hora y el lugar también son texto real, cada uno precedido por una etiqueta oculta visualmente (\"Time:\" / \"Location:\") con su icono oculto, así que ningún significado depende de un glifo. La imagen de cartel opcional es un <img> real con alt proporcionado por quien la usa, dentro de un pozo de relación de aspecto fija, de modo que la caja se dispone antes de que lleguen los bytes y nada salta al cargar. Cuando se define href, toda la tarjeta se convierte en UN enlace mediante el patrón de enlace extendido — un único <a> del título cuyo ::after cubre la tarjeta — y el control de Asistir se eleva con relative z-10 para seguir siendo clicable y enfocable de forma independiente; nunca se anida un <a> o <button> dentro de otro, y Enter/Espacio funcionan de forma nativa en ambos. Las tarjetas interactivas dibujan el anillo de foco tanto en :focus-within como en :focus-visible, así que tabular al enlace o al botón resalta toda la tarjeta; las tarjetas inertes lo omiten para no duplicar el anillo del botón. El control de Asistir es un <button> real con aria-pressed, una ETIQUETA que cambia con el estado (\"Attend\" → \"Attending\"), un icono que cambia de forma (calendario con más → calendario con marca) y una copia oculta del título del evento para distinguir el control de una tarjeta del de la siguiente; el relleno confirmado en contorno es una tercera señal redundante y nunca la única. El conteo de asistentes es texto real (\"27 going\"), derivado para contar a quien lee solo mientras asiste, y cada cambio se anuncia mediante una región aria-live=\"polite\" presente en el DOM antes de que llegue cualquier texto. No hay ninguna interacción de arrastre, así que nada depende solo del puntero. Bajo prefers-reduced-motion se suprimen el levantamiento de la tarjeta, el del talón, el zoom del cartel y el viaje del sello, conservando el estado final — el glifo de marca queda totalmente renderizado y el anillo de confirmación se elimina por completo. En forced-colors un borde con color de sistema mantiene los límites de la tarjeta una vez descartadas sombras y translucidez, se limpian el filtro de fondo del vidrio y el degradado del skeuo, la perforación decorativa descarta su imagen de fondo, los glifos se repintan a CanvasText, el control de Asistir presionado se repinta a Highlight/HighlightText y el anillo de foco a Highlight. Contraste: en cada material el título, el texto de la placa de fecha y las filas de metadatos atenuadas miden 4,5:1 o más contra su propia superficie, y la receta de vidrio lleva su propio tinte para que eso se cumpla igual sobre fondo blanco que negro. Las fechas nunca vienen del reloj — todas las cadenas de fecha y hora son props, así que una página generada estáticamente se hidrata sin discrepancias.",
  sourcePath: "src/registry/ui/event-card.tsx",
  Preview: EventCardPreview,
};
