import { WeatherCardPreview } from "@/registry/previews/weather-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the weather card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "weather-card",
  name: "Weather Card",
  nameEs: "Tarjeta del clima",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "Current conditions for one place: a large temperature reading, the condition spelled out in words beside a decorative glyph, a high/low pair, measurement tiles and a multi-day forecast strip — with a stretched-link pattern so the whole surface can lead to the full forecast without trapping the Save control.",
  descriptionEs:
    "Condiciones actuales de un lugar: una lectura de temperatura grande, la condición escrita en palabras junto a un glifo decorativo, un par de máxima y mínima, mosaicos de medidas y una tira de pronóstico de varios días — con un patrón de enlace extendido para que toda la superficie lleve al pronóstico completo sin atrapar el control de Guardar.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "The card is an <article> whose heading is the location, and the heading rank is a prop (headingLevel) because the correct level depends on the surrounding document outline. The condition is never carried by an icon or a colour alone: the words (\"Partly cloudy\") are real visible text and the weather glyph beside them is aria-hidden decoration. Every reading spells its unit for assistive tech — the visible \"24°\" is aria-hidden and an sr-only sibling reads \"24 degrees Celsius\", so the degree sign is never announced as punctuation; the same pairing covers the high, the low and each forecast day's high/low. The forecast is a real <ul> labelled by its visible heading, each <li> naming its weekday in text (a short form on screen with the full name sr-only, wrapped in <time dateTime> when a date is supplied) and its condition in words as well as a glyph. Measurement tiles carry a visible text label beside every value. The card ships no raster imagery: the glyphs are inline SVG at fixed pixel sizes, so nothing reflows on load and the layout is stable from first paint. When href is set the whole card becomes ONE link via the stretched-link pattern — a single <a> inside the heading, so the location is its accessible name, with an ::after overlay covering the card — while the Save control is raised on relative z-10 so it stays independently clickable and focusable; no <a> or <button> is ever nested inside another. The Save control is a real <button> operable with Enter and Space, and its state is carried by aria-pressed AND by its visible label (\"Save\" / \"Saved\") AND by a different icon, never by the accent fill alone; the confirmation is announced through an aria-live=\"polite\" region that is present in the DOM before any message arrives. Linked cards draw the focus ring on :focus-within as well as :focus-visible so tabbing to the link or the button outlines the whole card; inert cards skip that to avoid double-ringing the button. Under prefers-reduced-motion the hover lift, the forecast reveal and the glyph drift are all cancelled, and because both keyframe animations end at the resting state the card stays fully rendered. In forced-colors mode the decorative washes and the glass backdrop filter are cleared, system-coloured borders keep the card, its tiles and its divider visible, glyphs repaint to CanvasText, the pressed Save control repaints to Highlight/HighlightText and the focus ring to Highlight. Contrast: on every material the temperature, condition, labels, muted meta text and the weather glyphs measure at or above 4.5:1 against the surface they sit on — including the inset wells — and the glass recipe carries its own tint so that holds over a white and a black backdrop alike. No value is computed from the clock: temperatures, labels and timestamps all arrive as props, so a statically generated page and its hydration always agree.",
  a11yEs:
    "La tarjeta es un <article> cuyo encabezado es la ubicación, y el rango del encabezado es una prop (headingLevel) porque el nivel correcto depende del esquema del documento. La condición nunca se transmite solo con un icono o un color: las palabras (\"Partly cloudy\") son texto visible real y el glifo meteorológico contiguo es decoración aria-hidden. Cada lectura deletrea su unidad para la tecnología asistiva — el visible \"24°\" es aria-hidden y un hermano sr-only lee \"24 degrees Celsius\", de modo que el signo de grado nunca se anuncia como puntuación; el mismo emparejamiento cubre la máxima, la mínima y la máxima/mínima de cada día del pronóstico. El pronóstico es un <ul> real etiquetado por su encabezado visible, y cada <li> nombra su día de la semana en texto (forma corta en pantalla y nombre completo sr-only, envuelto en <time dateTime> cuando se aporta una fecha) y su condición en palabras además del glifo. Los mosaicos de medidas llevan una etiqueta de texto visible junto a cada valor. La tarjeta no incluye imágenes rasterizadas: los glifos son SVG en línea con tamaños fijos en píxeles, así que nada se recoloca al cargar y el diseño es estable desde el primer pintado. Cuando se define href, toda la tarjeta se convierte en UN enlace mediante el patrón de enlace extendido — un único <a> dentro del encabezado, para que la ubicación sea su nombre accesible, con un ::after que cubre la tarjeta — mientras el control de Guardar se eleva con relative z-10 para seguir siendo clicable y enfocable de forma independiente; nunca se anida un <a> o <button> dentro de otro. El control de Guardar es un <button> real operable con Enter y Espacio, y su estado va en aria-pressed Y en su etiqueta visible (\"Save\" / \"Saved\") Y en un icono distinto, nunca solo en el relleno de acento; la confirmación se anuncia mediante una región aria-live=\"polite\" presente en el DOM antes de que llegue cualquier mensaje. Las tarjetas enlazadas dibujan el anillo de foco tanto en :focus-within como en :focus-visible, así que tabular al enlace o al botón resalta toda la tarjeta; las tarjetas inertes lo omiten para no duplicar el anillo del botón. Bajo prefers-reduced-motion se cancelan el levantamiento al pasar el ratón, la aparición del pronóstico y el vaivén del glifo, y como ambas animaciones terminan en el estado de reposo la tarjeta queda completamente renderizada. En forced-colors se limpian los lavados decorativos y el desenfoque del vidrio, los bordes con color de sistema mantienen visibles la tarjeta, sus mosaicos y su divisor, los glifos se repintan a CanvasText, el control de Guardar presionado a Highlight/HighlightText y el anillo de foco a Highlight. Contraste: en cada material la temperatura, la condición, las etiquetas, el texto atenuado y los glifos miden 4,5:1 o más contra la superficie sobre la que están — incluidos los pozos hundidos — y la receta de vidrio lleva su propio tinte para que eso se cumpla igual sobre fondo blanco que negro. Ningún valor se calcula del reloj: temperaturas, etiquetas y marcas de tiempo llegan como props, así que la página generada estáticamente y su hidratación siempre coinciden.",
  sourcePath: "src/registry/ui/weather-card.tsx",
  Preview: WeatherCardPreview,
};
