import { TrendIndicatorPreview } from "@/registry/previews/trend-indicator-preview";
import type { RegistryEntry } from "@/registry/schema";

/** Registry entry for the self-contained Trend Indicator. */
export const entry: RegistryEntry = {
  slug: "trend-indicator",
  name: "Trend Indicator",
  nameEs: "Indicador de tendencia",
  category: "feedback",
  materials: ["adaptive"],
  description:
    "A compact KPI delta — direction arrow, signed value and an optional comparison caption — with an inverted semantic so a falling churn or latency figure reads as good news without ever contradicting the direction it states in words.",
  descriptionEs:
    "Un delta de KPI compacto —flecha de dirección, valor con signo y una leyenda de comparación opcional— con semántica invertida para que una caída de churn o de latencia se lea como buena noticia sin contradecir nunca la dirección que indica en palabras.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "lucide-react", "tailwind-merge"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    'The indicator is a readout with no interactive control, so it is exposed as a single role="img" whose aria-label spells the whole reading as one sentence — "Up 12.4 percent versus last month, improvement." — and every visual part inside it is additionally aria-hidden, so the figure is announced once and atomically instead of being assembled from stray fragments. That is the only accessible channel: there is no second sr-only sentence that could drift from the label. Direction and goodness are carried separately and redundantly, never by colour. DIRECTION travels on three non-colour channels — the arrow shape (rising, falling, or a flat bar), the sign character in the figure (+, the true minus sign, or none) and the word Up, Down or No change that opens the label. GOODNESS travels on its own qualifier word — improvement, decline or no change — which is always in the label and is rendered visibly whenever tone and direction disagree, which is exactly the inverted case (churn, error rate, latency) where a green downward arrow alone would mislead a sighted reader; showQualifier forces it on or off, and qualifier / qualifierLabel localise the wording. A caller can therefore never produce a green arrow that says nothing about why it is green. live defaults to off so a wall of static KPIs does not announce itself on mount; live="polite" upgrades the same node to an atomic polite live region for a figure refreshed in place, and urgent failures are deliberately left to an Alert with role="alert" rather than shouted from a number. Figures use tabular numerals so a column of deltas aligns digit for digit, and the magnitude formatter is locale-free and deterministic so a statically generated page and its hydration agree. Reduced motion: the directional entrance is expressed as keyframes on the standalone translate and scale properties whose END state is the resting state, so motion-reduce:animate-none leaves the indicator fully drawn at its true value rather than mid-flight or invisible; there is no transition anywhere, so nothing can animate against a property that never changes. forced-colors mode keeps the pill bounds with a CanvasText border, clears the decorative chip gradient and every depth shadow, and repaints the arrow, the figure and the caption as CanvasText — safe precisely because the meaning already lives in words; a forced focus ring uses Highlight. Contrast: the three tone hues are tuned to at least 4.5:1 against a light and a dark page surface because the figure they colour is real text, and both foregrounds (primary and muted caption) clear the same bar in both colour schemes. The component is not focusable by itself, but a focus-visible ring and a parallel data-[focus=true] ring are declared for callers that give it a tabIndex or wrap it in a link.',
  a11yEs:
    'El indicador es una lectura sin controles interactivos, así que se expone como un único role="img" cuyo aria-label enuncia toda la lectura como una frase —"Up 12.4 percent versus last month, improvement."— y cada parte visual interior lleva además aria-hidden, de modo que la cifra se anuncia una sola vez y de forma atómica en lugar de armarse con fragmentos sueltos. Ese es el único canal accesible: no hay una segunda frase sr-only que pueda desincronizarse de la etiqueta. La dirección y la bondad se transmiten por separado y de forma redundante, nunca por color. La DIRECCIÓN viaja en tres canales sin color: la forma de la flecha (ascendente, descendente o una barra plana), el signo en la cifra (+, el signo menos tipográfico o ninguno) y la palabra Up, Down o No change que abre la etiqueta. La BONDAD viaja en su propia palabra calificadora —improvement, decline o no change— que siempre está en la etiqueta y se muestra visualmente cuando el tono y la dirección no coinciden, es decir en el caso invertido (churn, tasa de error, latencia) donde una flecha verde hacia abajo por sí sola confundiría a quien mira; showQualifier la fuerza a mostrarse u ocultarse, y qualifier / qualifierLabel permiten traducir el texto. Así nunca se produce una flecha verde que no explique por qué es verde. live vale off por defecto para que un tablero de KPIs estáticos no se anuncie al montarse; live="polite" convierte ese mismo nodo en una región viva educada y atómica para una cifra que se refresca en su sitio, y los fallos urgentes se dejan deliberadamente a un Alert con role="alert" en vez de gritarlos desde un número. Las cifras usan numerales tabulares para que una columna de deltas quede alineada dígito a dígito, y el formateador de magnitud es determinista y sin dependencia de locale, por lo que una página generada estáticamente y su hidratación coinciden. Movimiento reducido: la entrada direccional se expresa con keyframes sobre las propiedades autónomas translate y scale cuyo estado FINAL es el estado en reposo, así que motion-reduce:animate-none deja el indicador completamente dibujado en su valor real y no a medio camino ni invisible; no hay ninguna transición, por lo que nada puede animar una propiedad que jamás cambia. El modo forced-colors conserva los límites de la píldora con un borde CanvasText, elimina el degradado decorativo del chip y todas las sombras de profundidad, y repinta la flecha, la cifra y la leyenda como CanvasText, algo seguro precisamente porque el significado ya vive en palabras; el anillo de foco forzado usa Highlight. Contraste: los tres tonos se ajustan a un mínimo de 4,5:1 sobre una superficie clara y una oscura porque la cifra que colorean es texto real, y ambos primeros planos (principal y leyenda atenuada) superan el mismo umbral en los dos esquemas de color. El componente no es enfocable por sí mismo, pero declara un anillo focus-visible y otro paralelo data-[focus=true] para quien le dé un tabIndex o lo envuelva en un enlace.',
  sourcePath: "src/registry/ui/trend-indicator.tsx",
  Preview: TrendIndicatorPreview,
};
