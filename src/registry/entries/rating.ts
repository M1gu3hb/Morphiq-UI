import { RatingPreview } from "@/registry/previews/rating-preview";
import type { RegistryEntry } from "@/registry/schema";

/** Registry metadata for the copy-and-own Rating. */
export const entry: RegistryEntry = {
  slug: "rating",
  name: "Rating",
  nameEs: "Calificación",
  category: "feedback",
  materials: ["adaptive"],
  description:
    "A star rating drawn by hand as inline SVG — click and hover-preview, full keyboard operation from a single tab stop, optional half steps, a configurable star count, and a passive read-only display mode — carrying role=\"slider\" with the reading spelled in aria-valuetext.",
  descriptionEs:
    "Una calificación por estrellas dibujada a mano en SVG en línea —clic y vista previa al pasar el puntero, manejo completo por teclado desde una sola parada de tabulación, medios pasos opcionales, número de estrellas configurable y un modo de solo lectura— que lleva role=\"slider\" con la lectura expresada en aria-valuetext.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "The interactive rating is a single role=\"slider\" carrying aria-valuemin=0, aria-valuemax equal to the star count, aria-valuenow and an aria-valuetext that spells the reading in words — \"3.5 out of 5 stars\", or \"No rating\" at zero — plus an aria-label naming the control; role, aria-valuemin, aria-valuemax and aria-valuenow are omitted from the public props type so a caller cannot desync them from value. The slider is a single tab stop and owns the whole keyboard contract: ArrowRight and ArrowUp add one step, ArrowLeft and ArrowDown subtract one, PageUp and PageDown move a whole star, Home sets the minimum (no rating) and End sets the maximum, and the step is 0.5 whenever allowHalf is set. Because role=\"slider\" has presentational children, every glyph inside it is decoration over the value the role already carries, and the label, the numeric readout and the status line are rendered as siblings of the slider rather than children of it. The read-only mode swaps to role=\"img\" with an aria-label that spells the same reading. Meaning never rests on colour: a filled star is a solid shape and an empty star is an outline, a half star is a genuine geometric clip of the solid shape inside a 50%-wide overflow window rather than an opacity fade, and the value is printed beside the row as real numeric text. The hover preview is pointer-only, never changes the committed value, is carried by a size change plus the visible word \"Preview\" and its number rather than by a hue, and clears on pointer leave, on commit and on any key press. Validation and busy state are text first: the message is a live region — role=\"alert\" with aria-live=\"assertive\" for an error, role=\"status\" with aria-live=\"polite\" for the busy notice, both aria-atomic, and overridable through the urgency prop — prefixed by an sr-only \"Error:\" or \"Status:\" so the state is named in words, accompanied by a decorative triangle glyph, and wired to the row through aria-describedby with aria-invalid set. While busy, the root carries aria-busy and the star row carries the inert attribute, so a keyboard user cannot tab into content sitting under the wash. Focus is always visible: focus-visible draws a two-pixel offset outline in the ring token, with a parallel data-focus hook so the documentation preview can force the same look, and forced-colors repaints it to Highlight. Reduced motion keeps the end state — the wipe that fills a star has no resting clip-path, so motion-reduce:animate-none leaves every filled star completely drawn, and the star's hover scale transition (declared on the standalone scale property Tailwind v4 writes, not on transform) is cancelled with motion-reduce:transition-none. In forced-colors mode the filled stars repaint to Highlight, the empty outlines to GrayText, the row keeps its bounds with a CanvasText border over Canvas, every label and message becomes CanvasText and the busy wash is cleared. Contrast holds at or above 4.5:1 on both schemes: the amber fill measures 5.2:1 on white and 9.8:1 on the dark surface, and the empty-star outline 5.1:1 and 6.9:1. Nothing in render reads the clock, the window or storage, so the server and the client always agree.",
  a11yEs:
    "La calificación interactiva es un único role=\"slider\" que lleva aria-valuemin=0, aria-valuemax igual al número de estrellas, aria-valuenow y un aria-valuetext que expresa la lectura en palabras —\"3.5 out of 5 stars\", o \"No rating\" en cero— además de un aria-label que nombra el control; role, aria-valuemin, aria-valuemax y aria-valuenow se omiten del tipo público de props para que quien lo consume no pueda desincronizarlos de value. El slider es una sola parada de tabulación y posee todo el contrato de teclado: ArrowRight y ArrowUp suman un paso, ArrowLeft y ArrowDown restan uno, PageUp y PageDown mueven una estrella entera, Home fija el mínimo (sin calificación) y End fija el máximo, y el paso es 0,5 cuando se activa allowHalf. Como role=\"slider\" tiene hijos presentacionales, cada glifo interior es decoración sobre el valor que el rol ya transmite, y la etiqueta, la lectura numérica y la línea de estado se renderizan como hermanos del slider y no como hijos suyos. El modo de solo lectura cambia a role=\"img\" con un aria-label que expresa la misma lectura. El significado nunca depende del color: una estrella llena es una forma sólida y una vacía es un contorno, la media estrella es un recorte geométrico real de la forma sólida dentro de una ventana con desbordamiento oculto al 50% y no un desvanecido de opacidad, y el valor se imprime junto a la fila como texto numérico real. La vista previa al pasar el puntero es solo de puntero, nunca cambia el valor confirmado, se transmite mediante un cambio de tamaño más la palabra visible \"Preview\" y su número en lugar de un tono, y se limpia al salir el puntero, al confirmar y ante cualquier tecla. La validación y el estado ocupado son texto ante todo: el mensaje es una región viva —role=\"alert\" con aria-live=\"assertive\" para un error, role=\"status\" con aria-live=\"polite\" para el aviso de guardado, ambos aria-atomic y sustituibles con la prop urgency— precedido por un \"Error:\" o \"Status:\" sr-only para nombrar el estado en palabras, acompañado de un glifo triangular decorativo y enlazado a la fila con aria-describedby y aria-invalid. Mientras está ocupado, la raíz lleva aria-busy y la fila de estrellas lleva el atributo inert, así que nadie puede tabular hacia el contenido que queda bajo el velo. El foco siempre es visible: focus-visible dibuja un contorno de dos píxeles con desplazamiento en el token de anillo, con un gancho paralelo data-focus para que la vista previa de la documentación fuerce el mismo aspecto, y forced-colors lo repinta a Highlight. El movimiento reducido conserva el estado final: el barrido que rellena una estrella no tiene clip-path en reposo, así que motion-reduce:animate-none deja cada estrella llena completamente dibujada, y la transición de escala al pasar el puntero (declarada sobre la propiedad autónoma scale que escribe Tailwind v4, no sobre transform) se cancela con motion-reduce:transition-none. En modo forced-colors las estrellas llenas se repintan a Highlight, los contornos vacíos a GrayText, la fila conserva sus límites con un borde CanvasText sobre Canvas, cada etiqueta y mensaje pasa a CanvasText y el velo de ocupado se elimina. El contraste se mantiene en 4,5:1 o más en ambos esquemas: el relleno ámbar mide 5,2:1 sobre blanco y 9,8:1 sobre la superficie oscura, y el contorno de la estrella vacía 5,1:1 y 6,9:1. Nada en el render lee el reloj, la ventana ni el almacenamiento, así que el servidor y el cliente siempre coinciden.",
  sourcePath: "src/registry/ui/rating.tsx",
  Preview: RatingPreview,
};
