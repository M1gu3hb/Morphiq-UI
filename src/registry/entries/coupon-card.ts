import { CouponCardPreview } from "@/registry/previews/coupon-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the coupon card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "coupon-card",
  name: "Coupon Card",
  nameEs: "Tarjeta de cupón",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A voucher shaped like a real ticket — notches punched into its edges by a CSS mask rather than an image — carrying a headline value, the fine print, an expiry and a promo code that can be revealed and copied: the code is real selectable text, the copy button names it, and success is announced through a live region.",
  descriptionEs:
    "Un vale con forma de billete real — con muescas recortadas en sus bordes mediante una máscara CSS en lugar de una imagen — que lleva un valor destacado, la letra pequeña, una fecha de caducidad y un código promocional que se puede revelar y copiar: el código es texto real seleccionable, el botón de copiar lo nombra y el éxito se anuncia mediante una región activa.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Each coupon is an <article> whose offer title is a real heading; the rank is set through headingLevel (default h3) rather than hardcoded, because the correct level depends on the surrounding document outline. The ticket silhouette — the two punched notches and the perforated tear line — is a CSS radial-gradient MASK plus an aria-hidden dashed rule, not artwork: there is no bitmap to load, nothing that needs alt text, and no box whose dimensions have to be reserved, so the card cannot shift on load and it keeps its shape over any backdrop, including a photograph. The headline value, the kicker, the terms, the expiry and the promo code are all REAL text rather than baked into an image, so they zoom, reflow, translate and are read verbatim; the expiry is a <time dateTime> whose machine-readable value arrives from props and never from new Date(), which keeps the render deterministic. When href is set the heading contains a single <a> whose transparent ::after overlay at inset-0 z-[1] turns the whole ticket into one link with the offer title as its accessible name, and everything that must stay reachable sits at relative z-10 ABOVE that overlay — the code well, the Reveal toggle and the Copy button — so the code stays selectable with pointer and keyboard while both controls stay independently clickable and focusable; no <a> or <button> is ever nested inside another. Linked coupons draw the focus ring on :focus-within as well as :focus-visible, so tabbing to the title outlines the whole ticket; inert coupons skip that to avoid double-ringing the Copy button. Keyboard: every control is reachable with Tab in DOM order and both are real <button type=\"button\">s, so Enter and Space activate them natively; there is no drag and no pointer-only gesture anywhere in the component, and because the code is selectable text, Shift+Arrow plus the platform copy shortcut is a complete keyboard equivalent to the Copy button — which is itself the pointer-free path to the clipboard. The Reveal toggle carries aria-expanded together with aria-controls pointing at the code well, and BOTH its visible label and its icon change (Reveal code / Hide code, Eye / EyeOff), so the state never rests on colour or on an icon alone; while hidden, the code is genuinely absent from the DOM and therefore from the accessibility tree — the bullet placeholder is aria-hidden and a screen-reader-only \"Hidden\" stands in — so assistive technology can never read out a code the eye cannot see. The Copy button's accessible name always contains its visible label and names the code itself (\"Copy code CLAY25\", then \"Copied code CLAY25\"), which satisfies label-in-name for voice control, and success is announced through a polite aria-live region with role=\"status\" that is rendered from the first paint, before any text is put into it, and kept outside every control so its text is never folded into a button's name; a clipboard write that is denied or unavailable announces nothing and leaves the idle label intact rather than claiming a copy that never happened. The two labels share one grid cell, so the button sizes to the wider of them and never reflows on the swap. Status words such as \"Last day\" and the kicker are text, so no state is signalled by colour alone. Under prefers-reduced-motion the hover lift, the press sink, the reveal rise and the confirmation stamp are all cancelled while their END states remain, so the coupon stays fully rendered and legible without any travel. In forced-colors mode a CanvasText border keeps the ticket's bounds once shadows, translucency and even the mask itself are discarded, the value-band wash and the skeuo gradient are cleared to none, the glass backdrop blur is dropped, glyphs and the tear line repaint to CanvasText, the code well falls back to Canvas / CanvasText, the controls fall back to ButtonFace / ButtonText and the focus ring becomes Highlight. Contrast: on every material the value, title, terms, expiry and code measure at or above 4.5:1 against the surface, the glass tokens carry their own tint so that holds over a white and a black backdrop alike, and the Copy control's foreground is verified a second time against its accent fill.",
  a11yEs:
    "Cada cupón es un <article> cuyo título de la oferta es un encabezado real; el rango se define con headingLevel (h3 por defecto) en vez de fijarse en el código, porque el nivel correcto depende del esquema del documento. La silueta del billete — las dos muescas troqueladas y la línea de corte perforada — es una MÁSCARA CSS de degradado radial más una regla discontinua aria-hidden, no una ilustración: no hay ningún mapa de bits que cargar, nada que necesite texto alternativo y ninguna caja cuyas dimensiones haya que reservar, así que la tarjeta no puede desplazarse al cargar y conserva su forma sobre cualquier fondo, incluida una fotografía. El valor destacado, el antetítulo, las condiciones, la caducidad y el código promocional son TEXTO real y no están grabados en una imagen, de modo que se amplían, se reflujan, se traducen y se leen literalmente; la caducidad es un <time dateTime> cuyo valor legible por máquina llega por props y nunca de new Date(), lo que mantiene el renderizado determinista. Cuando se define href, el encabezado contiene un único <a> cuya capa ::after transparente en inset-0 z-[1] convierte todo el billete en un solo enlace con el título de la oferta como nombre accesible, y todo lo que debe seguir siendo alcanzable se sitúa en relative z-10 POR ENCIMA de esa capa — el pozo del código, el conmutador de Revelar y el botón de Copiar — así que el código sigue siendo seleccionable con puntero y con teclado mientras ambos controles siguen siendo clicables y enfocables de forma independiente; nunca se anida un <a> o un <button> dentro de otro. Los cupones enlazados dibujan el anillo de foco tanto en :focus-within como en :focus-visible, así que tabular al título resalta todo el billete; los cupones inertes lo omiten para no duplicar el anillo del botón de Copiar. Teclado: todos los controles se alcanzan con Tab en el orden del DOM y ambos son <button type=\"button\"> reales, así que Enter y Espacio los activan de forma nativa; no hay ningún arrastre ni gesto exclusivo de puntero en el componente, y como el código es texto seleccionable, Mayús+Flecha junto al atajo de copiar del sistema es un equivalente completo de teclado al botón de Copiar — que es a su vez la vía al portapapeles sin puntero. El conmutador de Revelar lleva aria-expanded junto con aria-controls apuntando al pozo del código, y cambian TANTO su etiqueta visible como su icono (Revelar código / Ocultar código, Eye / EyeOff), así que el estado nunca depende solo del color ni solo de un icono; mientras está oculto, el código está genuinamente ausente del DOM y por tanto del árbol de accesibilidad — el marcador de puntos es aria-hidden y lo sustituye un \"Hidden\" solo para lectores de pantalla — de modo que la tecnología asistiva nunca puede leer un código que el ojo no ve. El nombre accesible del botón de Copiar contiene siempre su etiqueta visible y nombra el propio código (\"Copy code CLAY25\", y luego \"Copied code CLAY25\"), lo que cumple la regla de etiqueta en el nombre para el control por voz, y el éxito se anuncia mediante una región aria-live cortés con role=\"status\" presente desde el primer renderizado, antes de que se le ponga cualquier texto, y mantenida fuera de todo control para que su texto nunca se funda con el nombre de un botón; una escritura al portapapeles denegada o no disponible no anuncia nada y deja intacta la etiqueta de reposo en lugar de afirmar una copia que nunca ocurrió. Las dos etiquetas comparten una misma celda de rejilla, así que el botón se dimensiona según la más ancha y nunca se reajusta al intercambiarlas. Palabras de estado como \"Last day\" y el antetítulo son texto, así que ningún estado se señala solo con color. Con prefers-reduced-motion se cancelan el levantamiento al pasar el ratón, el hundimiento al pulsar, la aparición del código y el sello de confirmación, conservando sus estados FINALES, así que el cupón queda completamente renderizado y legible sin ningún recorrido. En forced-colors, un borde CanvasText mantiene los límites del billete cuando se descartan sombras, translucidez e incluso la propia máscara, se limpian a none el lavado de la banda de valor y el degradado de skeuo, se elimina el desenfoque del vidrio, los glifos y la línea de corte se repintan en CanvasText, el pozo del código recurre a Canvas / CanvasText, los controles recurren a ButtonFace / ButtonText y el anillo de foco pasa a Highlight. Contraste: en cada material el valor, el título, las condiciones, la caducidad y el código miden 4,5:1 o más contra la superficie, los tokens de vidrio llevan su propio tinte para que eso se cumpla igual sobre fondo blanco que negro, y el primer plano del control de Copiar se verifica una segunda vez contra su relleno de acento.",
  sourcePath: "src/registry/ui/coupon-card.tsx",
  Preview: CouponCardPreview,
};
