import { SegmentedNavPreview } from "@/registry/previews/segmented-nav-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the segmented-nav component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 */
export const entry: RegistryEntry = {
  slug: "segmented-nav",
  name: "Segmented Nav",
  nameEs: "Navegación segmentada",
  category: "navigation",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A single-select segmented control for switching between views: one shallow trough holding a row of flush segments, with a raised chip that slides to whichever segment is selected. The chip is measured from the live geometry of the selected segment, so it tracks reflow, late webfonts and coarse-pointer touch targets, and a hairline seam between neighbours retracts around it the way a physical segmented switch loses its line under the slider. Segments are real buttons in a tablist with roving tabindex and arrow-key selection, optionally wired to panels through aria-controls. Uncontrolled by default, controllable by value, with four material recipes and three sizes.",
  descriptionEs:
    "Un control segmentado de selección única para cambiar de vista: una cubeta poco profunda que aloja una fila de segmentos a ras, con una pastilla elevada que se desliza hasta el segmento seleccionado. La pastilla se mide a partir de la geometría real del segmento seleccionado, así que sigue los reflujos, las fuentes web tardías y los objetivos táctiles ampliados, y la costura de un píxel entre vecinos se retrae a su alrededor igual que un conmutador segmentado físico pierde su línea bajo el deslizador. Los segmentos son botones reales dentro de un tablist con tabindex móvil y selección con flechas, opcionalmente enlazados a paneles mediante aria-controls. No controlado por defecto, controlable por valor, con cuatro recetas de material y tres tamaños.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "The control is a role=\"tablist\" with an accessible label and an explicit horizontal orientation. It is deliberately not a nav landmark and its segments are deliberately not links: a segment does not go anywhere, it changes which view is shown in place, so the ARIA answer is state-only role=\"tab\" controls rather than anchors inside a navigation region — a nav landmark here would announce destinations that do not exist. Each segment is a real <button type=\"button\">, so Enter and Space activate it natively with no synthetic key handling, and each carries aria-selected. When a segment reveals a panel, pass its id and it is wired as aria-controls; the documented contract, which the shipped preview follows, is that aria-controls is only ever supplied while that panel is mounted, so the docs demo keeps every panel in the DOM and hides the unselected ones instead of unmounting them. Keyboard: exactly one segment is in the page tab order (the selected one, or the first enabled segment when nothing matches), so Tab reaches the control and Tab leaves it; ArrowLeft and ArrowRight move the selection and wrap around the ends, Home and End jump to the first and last, and every one of those moves focus onto the newly selected segment, which is the roving contract for automatic activation. Disabled segments are skipped by arrow navigation rather than landed on. Focus is moved from the key handler that caused the change, never from an effect, so the DOM focus and the selected state can never drift apart. There is no overlay, popup or disclosed region in this component, so there is nothing for Escape to close and no focus to return — Escape is left entirely to whatever dialog or menu the control may sit inside. The selected segment is marked three ways that do not depend on the sliding chip: aria-selected, a heavier font weight, and a bottom rule that is always reserved in the box model (transparent until it is needed), so the state survives before hydration, with JavaScript off, and once fills are discarded. The chip itself and the hairline seams are decoration and never enter the accessibility tree; any leading icon is aria-hidden so the label is the sole accessible name. Focus shows a 2px offset ring on :focus-visible using the material's ring token and is never removed; the docs surface can force that ring onto the selected segment through a parallel data-focus hook. Motion is suppressed under prefers-reduced-motion: the chip appears directly at the selected segment with no travel and the seam opacity swaps instantly, and because the chip is armed only after its first measurement it never slides in from a corner even on first paint. Coarse pointers get a segment at least 44px tall, and the chip follows because it is measured from the live box rather than from the size token. In forced-colors mode fills, gradients, backdrop blur and shadows are discarded, so the trough and the seams keep system-colour bounds, the chip is pinned to Canvas so the CanvasText label on it keeps full contrast, the selected segment is marked with CanvasText on its reserved rule, and the focus ring switches to the Highlight system colour. Contrast: on every material the unselected label measures at or above 4.5:1 against the trough and the selected label at or above 4.5:1 against the chip; the adaptive recipe also ships a dark-scheme palette that holds the same ratios. Disabled segments fade to 45%, which WCAG exempts from contrast as inactive controls.",
  a11yEs:
    "El control es un role=\"tablist\" con etiqueta accesible y orientación horizontal explícita. Deliberadamente no es una región de navegación y sus segmentos deliberadamente no son enlaces: un segmento no lleva a ninguna parte, cambia qué vista se muestra en el sitio, así que la respuesta ARIA son controles de estado role=\"tab\" y no anclas dentro de una región de navegación — una región nav aquí anunciaría destinos que no existen. Cada segmento es un <button type=\"button\"> real, así que Enter y Espacio lo activan de forma nativa sin manejo sintético de teclas, y cada uno lleva aria-selected. Cuando un segmento revela un panel se pasa su id y se conecta como aria-controls; el contrato documentado, que la vista previa publicada respeta, es que aria-controls solo se suministra mientras ese panel está montado, por eso la demostración mantiene todos los paneles en el DOM y oculta los no seleccionados en vez de desmontarlos. Teclado: exactamente un segmento está en el orden de tabulación de la página (el seleccionado, o el primero habilitado cuando ninguno coincide), así que Tab llega al control y Tab lo abandona; FlechaIzquierda y FlechaDerecha mueven la selección con retorno circular en los extremos, Inicio y Fin saltan al primero y al último, y todos esos movimientos llevan el foco al segmento recién seleccionado, que es el contrato del tabindex móvil con activación automática. Los segmentos deshabilitados se omiten en la navegación con flechas en lugar de recibir el foco. El foco se mueve desde el manejador de teclas que causó el cambio, nunca desde un efecto, así que el foco del DOM y el estado seleccionado no pueden separarse. Este componente no tiene superposición, ventana emergente ni región desplegable, así que no hay nada que Escape deba cerrar ni foco que devolver: Escape queda por completo para el diálogo o menú en el que el control pueda estar. El segmento seleccionado se marca de tres formas que no dependen de la pastilla deslizante: aria-selected, un peso tipográfico más fuerte y una regla inferior siempre reservada en el modelo de caja (transparente hasta que hace falta), así que el estado sobrevive antes de la hidratación, con JavaScript desactivado y cuando se descartan los rellenos. La pastilla y las costuras son decoración y nunca entran en el árbol de accesibilidad; cualquier icono inicial es aria-hidden para que la etiqueta sea el único nombre accesible. El foco muestra un anillo de 2px con desplazamiento en :focus-visible usando el token de anillo del material y nunca se elimina; la superficie de documentación puede forzar ese anillo sobre el segmento seleccionado mediante un gancho paralelo data-focus. El movimiento se suprime bajo prefers-reduced-motion: la pastilla aparece directamente en el segmento seleccionado sin recorrido y la opacidad de la costura cambia al instante y, como la pastilla se arma solo tras su primera medición, nunca llega desde una esquina ni en la primera pintura. Los punteros gruesos reciben un segmento de al menos 44px de alto, y la pastilla lo sigue porque se mide de la caja real y no del token de tamaño. En forced-colors se descartan rellenos, degradados, desenfoque y sombras, así que la cubeta y las costuras conservan límites de color de sistema, la pastilla se fija a Canvas para que la etiqueta CanvasText que lleva encima mantenga todo el contraste, el segmento seleccionado se marca con CanvasText en su regla reservada, y el anillo de foco cambia al color de sistema Highlight. Contraste: en cada material la etiqueta no seleccionada mide 4,5:1 o más contra la cubeta y la seleccionada 4,5:1 o más contra la pastilla; la receta adaptativa además incluye una paleta para esquema oscuro que mantiene las mismas proporciones. Los segmentos deshabilitados bajan al 45%, que WCAG exime de contraste por ser controles inactivos.",
  sourcePath: "src/registry/ui/segmented-nav.tsx",
  Preview: SegmentedNavPreview,
};
