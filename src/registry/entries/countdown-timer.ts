import { CountdownTimerPreview } from "@/registry/previews/countdown-timer-preview";
import type { RegistryEntry } from "@/registry/schema";

/** Registry entry for the self-contained Countdown Timer component. */
export const entry: RegistryEntry = {
  slug: "countdown-timer",
  name: "Countdown Timer",
  nameEs: "Temporizador de cuenta regresiva",
  category: "feedback",
  materials: ["adaptive"],
  description:
    "A split-flap countdown to a target instant or a fixed duration, with suppressible leading unit groups, an optional fraction-remaining dial, real pause and resume controls, and a completed state that fires onComplete once.",
  descriptionEs:
    "Una cuenta regresiva tipo panel abatible hacia un instante objetivo o una duración fija, con grupos de unidades iniciales suprimibles, dial opcional de fracción restante, controles reales de pausa y reanudación y un estado completado que dispara onComplete una sola vez.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "lucide-react", "tailwind-merge"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    'The readout is a role="timer" region with aria-live="polite" and aria-atomic, because a countdown is a running clock rather than an alert: it never interrupts, and the announce="off" prop silences it entirely for decorative use. A per-second live region would flood a screen reader, so the frequently changing digits sit in an aria-hidden layer and the only text the region contributes is a single sr-only sentence derived from a coarsened remainder — once an hour above a day, every fifteen minutes above an hour, every minute above a minute, every ten seconds above ten seconds, and every second for the final ten plus completion. Between those boundaries the sentence is byte-identical, so the DOM node does not change and nothing is announced. Coarse readings are floored and introduced with "about" so the timer never over-promises time, while the final seconds round up to match the digits on screen. Digits are real tabular-numeral text, never images, and every group carries a visible unit plate (Days, Hrs, Min, Sec) plus a separator. State is never colour alone: pending, counting down, paused, completed and unavailable each get a distinct lucide glyph (Clock, Pause, CircleCheck, Ban), a distinct visible word, and a shape difference — a dashed panel outline when paused or unavailable, a bordered plate on the chip when completed — and the same state is spelled out in the sr-only sentence. While the first clock reading is still pending the readout carries aria-busy="true"; while disabled the controls wrapper is both inert and disabled, so a keyboard user cannot reach a frozen control. The optional pause and resume control is a real button whose accessible name changes between "Pause countdown" and "Resume countdown". Under prefers-reduced-motion the split-flap roll, the colon blink and the dial sweep are all cancelled with motion-reduce:animate-none and motion-reduce:transition-none, and because each keyframe and transition rests at its END state the digits still show the correct value and the dial still shows the correct geometry. Forced-colors mode keeps the panel and tile bounds with CanvasText borders, clears the tile face gradient (background images survive forced colours), repaints the dial track to GrayText and its value arc to Highlight, renders every glyph and digit in CanvasText, and draws focus with a Highlight outline. Focus rings are focus-visible with a 2px outline and a 3px offset, mirrored by a data-focus="true" variant that also cascades to the controls so a documentation surface can render the real focused look. Contrast: digits measure at least 10:1 against both tile faces, unit plates and the caption at least 6.8:1 against both panels, and the dial arc clears 3:1 as an informative graphic in both colour schemes.',
  a11yEs:
    'La lectura es una región role="timer" con aria-live="polite" y aria-atomic, porque una cuenta regresiva es un reloj en marcha y no una alerta: nunca interrumpe, y la prop announce="off" la silencia por completo para usos decorativos. Una región viva por segundo inundaría a un lector de pantalla, así que los dígitos que cambian constantemente viven en una capa con aria-hidden y el único texto que aporta la región es una frase sr-only derivada de un resto redondeado a intervalos gruesos: cada hora por encima de un día, cada quince minutos por encima de una hora, cada minuto por encima de un minuto, cada diez segundos por encima de diez y cada segundo en los diez finales más la finalización. Entre esos límites la frase es idéntica, así que el nodo del DOM no cambia y no se anuncia nada. Las lecturas gruesas se redondean hacia abajo y se introducen con "about", de modo que el temporizador nunca promete más tiempo del que queda, mientras que los segundos finales redondean hacia arriba para coincidir con los dígitos en pantalla. Los dígitos son texto real con numerales tabulares, nunca imágenes, y cada grupo lleva una placa de unidad visible (Days, Hrs, Min, Sec) y un separador. El estado nunca depende solo del color: pendiente, en cuenta, en pausa, completado y no disponible reciben cada uno un glifo lucide distinto (Clock, Pause, CircleCheck, Ban), una palabra visible distinta y una diferencia de forma —contorno discontinuo del panel en pausa o no disponible, placa con borde en el chip al completarse— y ese mismo estado se enuncia en la frase sr-only. Mientras la primera lectura del reloj sigue pendiente la lectura lleva aria-busy="true"; cuando está deshabilitado, el contenedor de controles es inert además de disabled, así que ningún usuario de teclado alcanza un control congelado. El control opcional de pausa y reanudación es un botón real cuyo nombre accesible alterna entre "Pause countdown" y "Resume countdown". Con prefers-reduced-motion se cancelan el giro tipo panel abatible, el parpadeo de los dos puntos y el barrido del dial mediante motion-reduce:animate-none y motion-reduce:transition-none, y como cada keyframe y cada transición reposan en su estado FINAL, los dígitos siguen mostrando el valor correcto y el dial la geometría correcta. El modo de colores forzados conserva los límites del panel y de las fichas con bordes CanvasText, elimina el degradado de la cara de cada ficha (las imágenes de fondo sobreviven a los colores forzados), repinta la pista del dial en GrayText y su arco de valor en Highlight, dibuja cada glifo y dígito en CanvasText y marca el foco con un contorno Highlight. Los anillos de foco usan focus-visible con contorno de 2px y desplazamiento de 3px, replicados por una variante data-focus="true" que además se propaga a los controles para que una superficie de documentación pueda mostrar el aspecto enfocado real. Contraste: los dígitos miden al menos 10:1 sobre ambas caras de ficha, las placas de unidad y el rótulo al menos 6,8:1 sobre ambos paneles, y el arco del dial supera 3:1 como grafismo informativo en los dos esquemas de color.',
  sourcePath: "src/registry/ui/countdown-timer.tsx",
  Preview: CountdownTimerPreview,
};
