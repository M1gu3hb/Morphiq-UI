import { PostCardPreview } from "@/registry/previews/post-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the post card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "post-card",
  name: "Post Card",
  nameEs: "Tarjeta de publicación",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A social post: author avatar, name, handle and a prop-supplied timestamp, the post body, optional media in a fixed aspect-ratio well, and an action row of like, comment and share whose counts are real text — with a stretched-link pattern so the whole surface can lead to the permalink without trapping the buttons.",
  descriptionEs:
    "Una publicación social: avatar, nombre y usuario del autor, una marca de tiempo recibida por props, el cuerpo del mensaje, medios opcionales en un contenedor de proporción fija y una fila de acciones de me gusta, comentar y compartir cuyos contadores son texto real — con un patrón de enlace extendido para que toda la superficie lleve al enlace permanente sin atrapar los botones.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Each post is an <article> with an accessible name of the form \"Post by <author>\", and the author's display name is a REAL heading whose rank is overridable via headingLevel because the correct level depends on the surrounding document outline. The timestamp is a <time dateTime> whose ISO value and human-readable label both arrive as props — nothing calls Date.now() or new Date() during render, so a statically generated page can never disagree with its own hydration; the fallback formatter slices the ISO string, which is timezone-stable. Every image has stable dimensions before it loads: the avatar is a fixed-size round well and the media sits in an aspect-ratio well (16/9, 4/3 or 1/1) around an object-cover <img>, so nothing below shifts when the bytes land. The avatar's alt defaults to \"\" because the author's name is announced immediately beside it, while the media takes a real descriptive alt. When href is set the whole card becomes ONE link via the stretched-link pattern — a single <a> INSIDE the heading, so the author's name is its accessible name (plus a visually hidden \", view post\" that states the link's purpose), with a ::after overlay covering the card — and the three action buttons sit at relative z-10 so they stay independently clickable and focusable; no <a> or <button> is ever nested inside another. A linked card draws the focus ring on :focus-within as well as :focus-visible, which is the only way that overlay link can show focus; an inert card skips it so a focused button is never double-ringed. Like is a real <button aria-pressed> whose visible label changes with the state (\"Like\" -> \"Liked\"), whose heart changes SHAPE (outline to filled), and whose count is real text in tabular numerals — the state is never carried by colour alone. Comment and share are real buttons whose accessible names contain their visible label and their visible count (\"Comment on this post, 12 comments\"). Enter and Space work natively on all three; there is no drag, hover or pointer-only affordance anywhere. A polite aria-live region is present in the DOM from the first render, empty, so the toggle result (\"Liked. 41 likes.\") is announced when it arrives. The verified marker pairs its glyph with visually hidden text, never colour alone. Under prefers-reduced-motion the hover lift, the media zoom and the heart pop are dropped while every end state — the filled heart, the updated count, the fully rendered card — remains. In forced-colors mode a CanvasText border keeps the card's bounds once shadows and translucency are discarded, the skeuo gradient and the glass backdrop blur are cleared by hand, the action buttons fall back to ButtonFace/ButtonText and a pressed like to Highlight/HighlightText with the heart inheriting that forced foreground, and the focus ring repaints to Highlight. Contrast: on every material the name, body copy, muted handle/timestamp and action labels measure at or above 4.5:1 against the surface, the glass tokens carry their own tint so that holds over a white and a black backdrop alike, and the filled-heart tint clears 4.5:1 against both the surface and the pressed wash.",
  a11yEs:
    "Cada publicación es un <article> con un nombre accesible del tipo «Post by <autor>», y el nombre del autor es un encabezado REAL cuyo rango es configurable mediante headingLevel, porque el nivel correcto depende del esquema del documento circundante. La marca de tiempo es un <time dateTime> cuyo valor ISO y cuya etiqueta legible llegan ambos por props — nunca se llama a Date.now() ni a new Date() durante el render, así que una página generada estáticamente no puede contradecir su propia hidratación; el formateador de respaldo recorta la cadena ISO, lo cual es estable respecto a la zona horaria. Todas las imágenes tienen dimensiones estables antes de cargarse: el avatar es un pozo redondo de tamaño fijo y los medios ocupan un contenedor de proporción fija (16/9, 4/3 o 1/1) alrededor de un <img> con object-cover, así que nada se desplaza cuando llegan los bytes. El alt del avatar es \"\" por defecto porque el nombre del autor se anuncia justo al lado, mientras que los medios reciben un alt descriptivo real. Cuando se define href, toda la tarjeta se convierte en UN enlace mediante el patrón de enlace extendido — un único <a> DENTRO del encabezado, de modo que el nombre del autor es su nombre accesible (más un «, view post» oculto visualmente que declara el propósito del enlace), con un ::after que cubre la tarjeta — y los tres botones de acción se sitúan en relative z-10 para seguir siendo clicables y enfocables de forma independiente; nunca se anida un <a> o un <button> dentro de otro. Una tarjeta enlazada dibuja el anillo de foco en :focus-within además de :focus-visible, que es la única forma en que ese enlace superpuesto puede mostrar el foco; una tarjeta inerte lo omite para que un botón enfocado nunca reciba doble anillo. Me gusta es un <button aria-pressed> real cuya etiqueta visible cambia con el estado («Like» -> «Liked»), cuyo corazón cambia de FORMA (contorno a relleno) y cuyo contador es texto real en cifras tabulares — el estado nunca se transmite solo con color. Comentar y compartir son botones reales cuyos nombres accesibles contienen su etiqueta visible y su contador visible («Comment on this post, 12 comments»). Enter y Espacio funcionan de forma nativa en los tres; no hay arrastre ni ninguna afordancia exclusiva de puntero. Una región aria-live=\"polite\" está presente en el DOM desde el primer render, vacía, para que el resultado del cambio («Liked. 41 likes.») se anuncie al llegar. La marca de verificación acompaña su glifo con texto oculto visualmente, nunca solo color. Con prefers-reduced-motion se eliminan el levantamiento al pasar el ratón, el zoom de los medios y el rebote del corazón, mientras que todos los estados finales — el corazón relleno, el contador actualizado, la tarjeta completamente renderizada — permanecen. En forced-colors, un borde CanvasText mantiene los límites de la tarjeta una vez descartadas sombras y translucidez, el degradado de skeuo y el desenfoque de vidrio se limpian a mano, los botones de acción recurren a ButtonFace/ButtonText y un me gusta activado a Highlight/HighlightText con el corazón heredando ese primer plano forzado, y el anillo de foco se repinta con Highlight. Contraste: en cada material el nombre, el cuerpo, el usuario y la marca de tiempo atenuados y las etiquetas de acción miden 4,5:1 o más contra la superficie, los tokens de vidrio llevan su propio tinte para que eso se cumpla igual sobre fondo blanco que negro, y el tinte del corazón relleno supera 4,5:1 tanto sobre la superficie como sobre el fondo del estado activado.",
  sourcePath: "src/registry/ui/post-card.tsx",
  Preview: PostCardPreview,
};
