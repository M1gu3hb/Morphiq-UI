import { SwipeCardsPreview } from "@/registry/previews/swipe-cards-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the swipe cards component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "swipe-cards",
  name: "Swipe Cards",
  nameEs: "Tarjetas deslizables",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A deck triaged one card at a time: drag the top card left to reject or right to accept, and watch it fly out to reveal the next. Dragging is never the only way — real Reject and Accept buttons sit under the deck, the left and right arrow keys do the same job, a progress bar and a counter track how far you are, and every decision is announced in a live region.",
  descriptionEs:
    "Una baraja que se revisa carta a carta: arrastra la tarjeta superior a la izquierda para rechazar o a la derecha para aceptar y verás cómo sale volando para dejar ver la siguiente. Arrastrar nunca es la única vía — botones reales de Rechazar y Aceptar debajo de la baraja, las flechas izquierda y derecha hacen lo mismo, una barra de progreso y un contador indican por dónde vas, y cada decisión se anuncia en una región activa.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "The deck is a <section> with an accessible name from label, and every card in it is an <article> labelled by its own title through aria-labelledby; the title is a real heading whose rank is set with headingLevel (default h3) rather than hardcoded, because the correct level depends on the surrounding document outline. Optional artwork is a real <img> with descriptive alt text from imageAlt inside an aspect-[4/3] well with a definite max height, and the whole stack lives in a well of definite height, so promoting the next card to the top reserves its exact box and never reflows the page around it; the image is loading=\"lazy\", decoding=\"async\" and draggable={false} so a picture drag can never fight the card drag. When href is set the heading contains a single <a>, so the title text IS the link's accessible name — and it is deliberately NOT given the stretched-link ::after overlay used elsewhere in this library, because a full-bleed overlay would swallow pointerdown and make the gesture impossible; the link sits at relative z-10 above the drag layer instead, independently clickable and independently focusable, and no <a> or <button> is ever nested inside another. DRAG IS NEVER THE ONLY WAY, which is the whole keyboard contract: two real <button>s (Reject / Accept) sit in a tray under the deck and do exactly what a swipe does, ArrowLeft rejects and ArrowRight accepts anywhere in the region, and Enter and Space activate whichever control has focus natively — nothing here fakes a button. Arrow keys yield first to inputs, textareas, selects, sliders and contenteditable regions, and a pointer press that starts on a link or a control inside the card is never hijacked into a drag. Focus is never dropped: the decision buttons live OUTSIDE the stack, so the leaving card cannot unmount the focused element; if focus does sit on a link inside that card it is moved deliberately onto the control that made the decision; and when the last card leaves — which disables both decision buttons, and a browser blurs a disabled element — focus is reclaimed onto \"Start over\" only when it really was inside the deck and really has fallen to <body>. Cards beneath the top one are both inert and aria-hidden, so neither Tab nor a virtual cursor can reach a card that cannot be seen or acted on. No control in this component toggles, so nothing carries aria-pressed; state is carried instead by an aria-live=\"polite\" role=\"status\" region that is rendered empty from the first paint — so each \"Accepted <title>. 3 of 4 remaining.\" arrives into a region assistive tech is already watching — by a role=\"progressbar\" with aria-valuemin, aria-valuemax, aria-valuenow and an aria-valuetext of \"N of M reviewed\", and by a visible \"Card N of M\" counter in plain text. The intent stamps that fade in mid-drag are aria-hidden decoration duplicated by that announcement, and they carry a WORD and an icon, never a colour alone; tags are text for the same reason. Under prefers-reduced-motion, which is read at the moment a decision is made rather than at module scope, the fling is skipped entirely: the card simply goes, the next one is shown and the announcement does the work, while the card, control and progress transitions and the empty-state entrance animation are cancelled with their END states intact, so nothing is stranded mid-travel. In forced-colors mode a CanvasText border keeps each card's bounds once shadows, gradients and translucency are discarded, the glass backdrop blur is dropped, glyphs repaint to CanvasText, the two decision buttons fall back to ButtonFace / ButtonText, the progress track becomes GrayText with a Highlight fill, and the focus ring becomes Highlight. That ring is declared for real :focus-visible and identically for a data-focus=\"true\" attribute, so documentation and visual-regression runs can render the focused look without synthesising a key event. Contrast: on every material the title, meta, description and counter measure at or above 4.5:1 against the surface, the glass tokens carry their own tint so that holds over a white and a black backdrop alike, and the accept and reject accents clear 4.5:1 against their own surface while never carrying meaning on their own — every decision is spelled out in words.",
  a11yEs:
    "La baraja es un <section> con nombre accesible tomado de label, y cada tarjeta es un <article> etiquetado por su propio título mediante aria-labelledby; el título es un encabezado real cuyo rango se define con headingLevel (h3 por defecto) en vez de fijarse en el código, porque el nivel correcto depende del esquema del documento. La ilustración opcional es un <img> real con texto alternativo descriptivo tomado de imageAlt, dentro de un pozo aspect-[4/3] con altura máxima definida, y toda la pila vive en un pozo de altura definida, así que promover la siguiente tarjeta reserva su caja exacta y nunca refluye la página; la imagen usa loading=\"lazy\", decoding=\"async\" y draggable={false} para que arrastrar la imagen no compita con el arrastre de la tarjeta. Cuando se define href, el encabezado contiene un único <a>, de modo que el texto del título ES el nombre accesible del enlace — y deliberadamente NO se le da la capa ::after de enlace extendido que se usa en otras tarjetas de la librería, porque una capa a sangre completa se tragaría el pointerdown y haría imposible el gesto; el enlace se sitúa en relative z-10 por encima de la capa de arrastre, clicable y enfocable de forma independiente, y nunca se anida un <a> o un <button> dentro de otro. ARRASTRAR NUNCA ES LA ÚNICA VÍA, y ese es todo el contrato de teclado: dos <button> reales (Rechazar / Aceptar) están en una bandeja bajo la baraja y hacen exactamente lo mismo que un deslizamiento, ArrowLeft rechaza y ArrowRight acepta en cualquier punto de la región, y Enter y Espacio activan de forma nativa el control enfocado — aquí nada simula un botón. Las flechas ceden primero a inputs, textareas, selects, sliders y regiones contenteditable, y una pulsación que empieza sobre un enlace o un control dentro de la tarjeta jamás se convierte en arrastre. El foco nunca se pierde: los botones de decisión viven FUERA de la pila, así que la tarjeta que sale no puede desmontar el elemento enfocado; si el foco estaba sobre un enlace de esa tarjeta se traslada a propósito al control que tomó la decisión; y cuando sale la última tarjeta — lo que deshabilita ambos botones, y el navegador desenfoca un elemento deshabilitado — el foco se recupera en \"Start over\" solo si realmente estaba dentro de la baraja y realmente cayó a <body>. Las tarjetas bajo la superior son inert y aria-hidden a la vez, así que ni el tabulador ni un cursor virtual pueden alcanzar una tarjeta que no se ve ni se puede accionar. Ningún control de este componente conmuta, así que nada lleva aria-pressed; el estado lo transmiten una región aria-live=\"polite\" con role=\"status\" renderizada vacía desde el primer pintado — para que cada \"Accepted <título>. 3 of 4 remaining.\" llegue a una región que la tecnología asistiva ya observa —, un role=\"progressbar\" con aria-valuemin, aria-valuemax, aria-valuenow y un aria-valuetext de \"N of M reviewed\", y un contador visible \"Card N of M\" en texto plano. Los sellos de intención que aparecen durante el arrastre son decoración aria-hidden duplicada por ese anuncio, y llevan una PALABRA y un icono, nunca solo un color; las etiquetas son texto por la misma razón. Con prefers-reduced-motion, que se consulta en el momento de decidir y no en el ámbito del módulo, se omite por completo el lanzamiento: la tarjeta simplemente se va, se muestra la siguiente y el anuncio hace el trabajo, mientras las transiciones de tarjeta, controles y progreso y la animación de entrada del estado vacío se cancelan conservando sus estados FINALES, así que nada queda a medio recorrido. En forced-colors, un borde CanvasText mantiene los límites de cada tarjeta cuando se descartan sombras, degradados y translucidez, se elimina el desenfoque del vidrio, los glifos se repintan en CanvasText, los dos botones de decisión recurren a ButtonFace / ButtonText, la pista de progreso pasa a GrayText con relleno Highlight y el anillo de foco pasa a Highlight. Ese anillo se declara para :focus-visible real y de forma idéntica para un atributo data-focus=\"true\", así que la documentación y las pruebas de regresión visual pueden mostrar el estado enfocado sin sintetizar un evento de teclado. Contraste: en cada material el título, el meta, la descripción y el contador miden 4,5:1 o más contra la superficie, los tokens de vidrio llevan su propio tinte para que eso se cumpla igual sobre fondo blanco que negro, y los acentos de aceptar y rechazar superan 4,5:1 contra su propia superficie sin llevar nunca el significado por sí solos — cada decisión se expresa con palabras.",
  sourcePath: "src/registry/ui/swipe-cards.tsx",
  Preview: SwipeCardsPreview,
};
