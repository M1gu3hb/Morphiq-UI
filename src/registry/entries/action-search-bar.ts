import { ActionSearchBarPreview } from "@/registry/previews/action-search-bar-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the action search bar component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 */
export const entry: RegistryEntry = {
  slug: "action-search-bar",
  name: "Action Search Bar",
  nameEs: "Barra de búsqueda de acciones",
  category: "inputs",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A search field that drops an opaque panel of filtered actions on focus and on typing. Implements the full ARIA 1.2 combobox: the caret stays put while the arrow keys steer a virtual cursor, Enter runs the active action, and a live region announces the result count. Each action carries an icon and an optional shortcut hint.",
  descriptionEs:
    "Un campo de búsqueda que despliega un panel opaco de acciones filtradas al enfocar y al escribir. Implementa el combobox completo de ARIA 1.2: el cursor de texto permanece fijo mientras las flechas mueven un cursor virtual, Enter ejecuta la acción activa y una región en vivo anuncia el número de resultados. Cada acción lleva un icono y una pista de atajo opcional.",
  variants: [{ id: "default", label: "Default", labelEs: "Por defecto" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "There is no native element for a text box that owns a steerable list of suggestions, so this is a hand-rolled ARIA 1.2 combobox rather than a wrapped native control. The text box carries role=combobox with aria-expanded, aria-controls pointing at the role=listbox, aria-autocomplete=list and, while a row is active, aria-activedescendant naming that row's id. DOM focus never leaves the input: ArrowUp and ArrowDown move a virtual cursor via aria-activedescendant so a screen reader reads the active row while the caret stays where the user is typing; Home and End jump to the first and last result. Enter runs the active row, a pointer press runs a row (its mousedown is prevented so the field never blurs mid-click), Escape dismisses the list and — once the list is already closed — clears the query, and blur closes the list. The listbox is labelled, each row exposes aria-selected, and a polite role=status live region announces how many results the query matched. The single source of truth for the error state is aria-invalid, and the error styling keys off that same attribute so what is shown and what assistive tech is told cannot drift apart; aria-describedby is composed rather than overwritten, so a caller's own description survives. Two message regions stay mounted even when empty — the result-count status and the helper/error line — because an aria-live region must exist before its text arrives for the announcement to be reliable. The active row is never signalled by colour alone: it gains a background, a label colour and an inline-start accent bar drawn as an inset shadow. Focus combines a 2px offset focus-visible outline with a material-specific inset well. prefers-reduced-motion removes the panel's slide and the well's interpolation but preserves the final state — the panel still opens, the row still highlights. forced-colors removes the tactile shadows, the material gradient and the backdrop filter while a system-coloured border and outline keep the field bounds, focus and invalid state perceivable, the panel is repainted on Canvas with a CanvasText border, and the active row is drawn in Highlight and HighlightText. The glass panel uses an opaque stand-in surface because a translucent popup would fail over a busy backdrop. Contrast: typed text and placeholder both measure at or above 4.5:1 against the input surface on every material — clay 9.81:1 / 4.80:1, glass 7.03:1 / 5.12:1, skeuo 9.55:1 / 5.47:1, adaptive light 14.98:1 / 6.59:1 and adaptive dark 12.24:1 / 7.01:1. Each row's label and its shortcut hint clear 4.5:1 against both the resting panel surface and the active-row background — shortcut hints measure 5.02:1 on clay's active row, 9.18:1 on glass, 5.75:1 on skeuo, and 6.25:1 (light) to 6.30:1 (dark) on adaptive — and the search glyph and row icons follow the row's own colour, so they inherit the same margins.",
  a11yEs:
    "No existe un elemento nativo para un campo de texto que gobierne una lista navegable de sugerencias, así que esto es un combobox ARIA 1.2 hecho a mano y no un control nativo envuelto. El campo lleva role=combobox con aria-expanded, aria-controls apuntando al role=listbox, aria-autocomplete=list y, mientras una fila está activa, aria-activedescendant nombrando el id de esa fila. El foco del DOM nunca abandona el input: ArrowUp y ArrowDown mueven un cursor virtual mediante aria-activedescendant para que un lector de pantalla lea la fila activa mientras el cursor de texto permanece donde se escribe; Home y End saltan al primer y último resultado. Enter ejecuta la fila activa, un clic ejecuta una fila (su mousedown se previene para que el campo no pierda el foco a mitad del clic), Escape descarta la lista y —una vez cerrada— borra la consulta, y el blur cierra la lista. El listbox está etiquetado, cada fila expone aria-selected, y una región en vivo role=status con cortesía anuncia cuántos resultados coincidieron. La única fuente de verdad del estado de error es aria-invalid, y el estilo de error se apoya en ese mismo atributo para que lo que se ve y lo que se le dice a la tecnología de asistencia no puedan desincronizarse; aria-describedby se compone en vez de sobrescribirse, así que una descripción propia de quien lo usa sobrevive. Dos regiones de mensaje permanecen montadas incluso vacías —el conteo de resultados y la línea de ayuda/error— porque una región aria-live debe existir antes de que llegue su texto para que el anuncio sea fiable. La fila activa nunca se señala solo con color: gana un fondo, un color de etiqueta y una barra de acento al inicio dibujada como sombra interior. El foco combina un contorno focus-visible de 2px desplazado con un pozo interior específico del material. prefers-reduced-motion elimina el deslizamiento del panel y la interpolación del pozo pero conserva el estado final: el panel sigue abriéndose y la fila sigue resaltándose. forced-colors quita las sombras táctiles, el gradiente del material y el filtro de fondo mientras un borde y contorno de sistema mantienen perceptibles los límites, el foco y el estado inválido, el panel se repinta sobre Canvas con borde CanvasText, y la fila activa se dibuja en Highlight y HighlightText. El panel glass usa una superficie opaca sustituta porque un popup translúcido fallaría sobre un fondo cargado. Contraste: tanto el texto escrito como el placeholder miden 4,5:1 o más contra la superficie del input en cada material — clay 9,81:1 / 4,80:1, glass 7,03:1 / 5,12:1, skeuo 9,55:1 / 5,47:1, adaptive claro 14,98:1 / 6,59:1 y adaptive oscuro 12,24:1 / 7,01:1. La etiqueta de cada fila y su pista de atajo superan 4,5:1 tanto contra la superficie en reposo del panel como contra el fondo de la fila activa — las pistas de atajo miden 5,02:1 en la fila activa de clay, 9,18:1 en glass, 5,75:1 en skeuo, y de 6,25:1 (claro) a 6,30:1 (oscuro) en adaptive — y el glifo de búsqueda y los iconos de fila siguen el color de la propia fila, heredando los mismos márgenes.",
  sourcePath: "src/registry/ui/action-search-bar.tsx",
  Preview: ActionSearchBarPreview,
};
