import { ProgressCardPreview } from "@/registry/previews/progress-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the progress card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "progress-card",
  name: "Progress Card",
  nameEs: "Tarjeta de progreso",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A goal card: an eyebrow, a semantic heading, a big current-vs-target metric, a real role=\"progressbar\" bar with quarter marks, and a status line — with the reading written out as text (\"$3,200 of $5,000\", \"64%\", \"On track\") so colour only ever reinforces it, plus a stretched-link pattern that keeps the footer action independently clickable.",
  descriptionEs:
    "Una tarjeta de objetivo: un antetítulo, un encabezado semántico, una métrica grande de actual frente a meta, una barra real con role=\"progressbar\" y marcas de cuartos, y una línea de estado — con la lectura escrita como texto (\"$3,200 of $5,000\", \"64%\", \"On track\") para que el color solo la refuerce, más un patrón de enlace extendido que mantiene la acción del pie clicable de forma independiente.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Each card is an <article> whose goal name is a real heading; the rank is set through headingLevel (default h3) rather than hardcoded, because the correct level depends on the surrounding document outline. The bar is a real role=\"progressbar\" element carrying aria-valuemin=0, aria-valuemax set to the target, aria-valuenow set to the current amount and an aria-valuetext that spells the whole reading (\"$3,200 of $5,000, 64 percent\"), and it is named by the heading through aria-labelledby, so a screen reader announces WHICH goal the number belongs to. Every one of those numbers is derived from the same clamped pair — value is pinned into [0, target] and a non-finite or non-positive target falls back to 100 — so the visible metric, the bar's width and the ARIA values can never disagree. Nothing is signalled by colour alone: the \"n of N\" metric, the rounded percentage and the status word (On track / Behind / Complete, overridable via statusLabel) are all real text, each status additionally gets its OWN glyph (trending arrow, warning triangle, check circle) rather than only a hue, and the status tints are measured at 5.1:1 or better on every material. The bar has no drag interaction to replace — it is a read-only progressbar, not a slider, so there is no pointer gesture without a keyboard equivalent; the only keyboard targets are the title link and the footer <button>, both reached by Tab and activated natively (Enter, plus Space on the button). When href is set the heading contains a single <a> whose transparent ::after overlay at inset-0 z-[1] makes the whole surface clickable, so the title text IS the accessible name of the card link, and the footer action sits at relative z-10 ABOVE that overlay to stay independently clickable and focusable; no <a> or <button> is ever nested inside another, and a disabled card drops the stretched link entirely and disables the action. Interactive cards draw the ring on :focus-within as well as :focus-visible, so tabbing to either control outlines the whole card; inert cards skip that to avoid double-ringing the button. An aria-live=\"polite\", aria-atomic region is in the DOM from the first paint, before any text is put into it, so a later confirmation pushed through announcement (\"Contribution added\") is actually announced instead of being read as a newly inserted region. The card ships no image, so there is no alt text to write; the geometry that would otherwise shift is reserved instead — the track holds a fixed thickness from --mq-bar per size, the fill is absolutely positioned inside it and grows only its own width, and the deadline is rendered inside a <time dateTime> from an ISO prop paired with a pre-formatted display string (never new Date() in render), so server and client markup match exactly and nothing below the bar reflows. Under prefers-reduced-motion the fill's grow-in keyframe and its width transition are both cancelled while the END state remains, so the bar is still painted at its correct value, and the hover lift and press sink are cancelled too. In forced-colors mode a CanvasText border keeps the card's bounds once shadows and translucency are discarded, the skeuo gradient is cleared and the glass backdrop blur is dropped, the track repaints to GrayText while the fill repaints to Highlight so the two stay distinguishable, the quarter marks and status glyphs become CanvasText, the action falls back to ButtonFace / ButtonText and the focus ring becomes Highlight. Contrast: on every material the heading, the metric, the percentage and the muted eyebrow, description and footer text all measure at or above 4.5:1 against the surface, and the glass tokens carry their own tint so that holds over a white and a black backdrop alike.",
  a11yEs:
    "Cada tarjeta es un <article> cuyo nombre de objetivo es un encabezado real; el rango se define con headingLevel (h3 por defecto) en vez de fijarse en el código, porque el nivel correcto depende del esquema del documento. La barra es un elemento real con role=\"progressbar\" que lleva aria-valuemin=0, aria-valuemax con la meta, aria-valuenow con la cantidad actual y un aria-valuetext que deletrea la lectura completa (\"$3,200 of $5,000, 64 percent\"), y se nombra desde el encabezado mediante aria-labelledby, de modo que un lector de pantalla anuncia A QUÉ objetivo pertenece el número. Todos esos números derivan del mismo par acotado — el valor se limita a [0, target] y una meta no finita o no positiva cae a 100 — así que la métrica visible, el ancho de la barra y los valores ARIA nunca pueden discrepar. Nada se señala solo con color: la métrica \"n de N\", el porcentaje redondeado y la palabra de estado (On track / Behind / Complete, configurable con statusLabel) son texto real, cada estado recibe además su PROPIO glifo (flecha de tendencia, triángulo de advertencia, círculo de verificación) y no solo un tono, y los tintes de estado miden 5,1:1 o más en cada material. La barra no tiene arrastre que reemplazar — es un progressbar de solo lectura, no un deslizador, así que no hay ningún gesto de puntero sin equivalente de teclado; los únicos destinos de teclado son el enlace del título y el <button> del pie, ambos alcanzables con Tab y activables de forma nativa (Enter, más Espacio en el botón). Cuando se define href, el encabezado contiene un único <a> cuya capa ::after transparente en inset-0 z-[1] hace clicable toda la superficie, de modo que el texto del título ES el nombre accesible del enlace de la tarjeta, y la acción del pie se sitúa en relative z-10 POR ENCIMA de esa capa para seguir siendo clicable y enfocable de forma independiente; nunca se anida un <a> o <button> dentro de otro, y una tarjeta deshabilitada elimina por completo el enlace extendido y desactiva la acción. Las tarjetas interactivas dibujan el anillo tanto en :focus-within como en :focus-visible, así que tabular a cualquiera de los dos controles resalta toda la tarjeta; las tarjetas inertes lo omiten para no duplicar el anillo del botón. Una región aria-live=\"polite\" con aria-atomic está en el DOM desde el primer renderizado, antes de que llegue cualquier texto, para que una confirmación posterior enviada por announcement (\"Contribution added\") se anuncie de verdad en lugar de leerse como una región recién insertada. La tarjeta no incluye imagen, así que no hay texto alternativo que escribir; en su lugar se reserva la geometría que podría desplazarse — la pista mantiene un grosor fijo desde --mq-bar según el tamaño, el relleno se posiciona en absoluto dentro de ella y solo crece su propio ancho, y la fecha límite se renderiza dentro de un <time dateTime> a partir de una prop ISO junto con una cadena de visualización ya formateada (nunca new Date() en el renderizado), de modo que el marcado del servidor y del cliente coinciden y nada bajo la barra se recoloca. Con prefers-reduced-motion se cancelan tanto el keyframe de crecimiento del relleno como su transición de ancho, conservando el estado FINAL, así que la barra sigue pintada en su valor correcto, y también se cancelan el levantamiento al pasar el ratón y el hundimiento al pulsar. En forced-colors, un borde CanvasText mantiene los límites de la tarjeta cuando se descartan sombras y translucidez, se limpia el degradado de skeuo y se elimina el desenfoque del vidrio, la pista se repinta en GrayText mientras el relleno se repinta en Highlight para que ambos sigan distinguiéndose, las marcas de cuartos y los glifos de estado pasan a CanvasText, la acción recurre a ButtonFace / ButtonText y el anillo de foco pasa a Highlight. Contraste: en cada material el encabezado, la métrica, el porcentaje y el texto atenuado del antetítulo, la descripción y el pie miden 4,5:1 o más contra la superficie, y los tokens de vidrio llevan su propio tinte para que eso se cumpla igual sobre fondo blanco que negro.",
  sourcePath: "src/registry/ui/progress-card.tsx",
  Preview: ProgressCardPreview,
};
