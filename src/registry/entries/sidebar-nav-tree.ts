import { SidebarNavTreePreview } from "@/registry/previews/sidebar-nav-tree-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the sidebar-nav-tree component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 */
export const entry: RegistryEntry = {
  slug: "sidebar-nav-tree",
  name: "Sidebar Nav Tree",
  nameEs: "Árbol de navegación lateral",
  category: "navigation",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "The nested, collapsible navigation a documentation site puts down its left edge: branches disclose child links and further sub-branches at arbitrary depth, rendered recursively from plain data with a depth prop driving the indent. Each branch is a real disclosure button over a nested list that eases open on grid-template-rows, a hairline guide traces every level and warms where it follows the path to the current page, and destinations stay real anchors. Four material recipes across three sizes.",
  descriptionEs:
    "La navegación anidada y plegable que un sitio de documentación coloca en su borde izquierdo: las ramas despliegan enlaces hijos y más subramas a profundidad arbitraria, renderizadas de forma recursiva a partir de datos planos con una prop de profundidad que gobierna la sangría. Cada rama es un botón de divulgación real sobre una lista anidada que se abre suavemente con grid-template-rows, una guía de un píxel traza cada nivel y se aviva donde sigue el camino hasta la página actual, y los destinos siguen siendo anclas reales. Cuatro recetas de material en tres tamaños.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "The tree is a nav landmark with an accessible label, and its structure is real list markup: nested ul/li at every level, each list given role=\"list\" so Safari cannot strip the semantics when the bullets are removed. It is deliberately NOT role=\"tree\" — this is navigation rather than a selection widget, so there is no roving tabindex and no typeahead, and Tab reaches every branch toggle and every link in source order. Destinations are real anchors when given an href and real buttons otherwise, so the browser owns activation, middle-click and open-in-new-tab; a leading folder or file glyph is aria-hidden, leaving the label as the sole accessible name. Every branch label is a button exposing aria-expanded and aria-controls pointing at its nested list, which stays mounted at all times so that reference is never dangling; while collapsed the list is inert, so it leaves the accessibility tree and the tab order and no keyboard user can land inside a closed branch. Enter and Space toggle a branch natively because the toggle is a real button; ArrowRight opens a closed branch and ArrowLeft closes an open one; Escape pressed anywhere inside a disclosed branch collapses that branch and returns focus to the toggle that opened it, stopping propagation so the innermost branch is the one that closes and so the key never also dismisses a drawer or dialog the tree sits inside. The current page carries aria-current=\"page\" together with a heavier weight, its own colour, a wash AND a reserved 3px left rule — a transparent border always held in the box model that only takes colour when active — so the state is never signalled by colour alone and marking it never shifts a row. The path down to it is legible too: every ancestor branch is expanded on first render (state is seeded during the initial render, so this holds before hydration and with JavaScript off) and marked with a heavier weight plus its own reserved rule in a muted tone that cannot be mistaken for the page itself. Rows are full-width targets at least 38px tall, 44px at md and 50px at lg. Focus shows a 2px inset ring on :focus-visible using the material's ring token and is never removed; a parallel data-focus hook lets the docs preview hold that ring open for inspection. Motion is suppressed under prefers-reduced-motion: the disclosure transition and the caret rotation are dropped while both end states survive untouched, so an open branch is still fully open and a closed one still closed. In forced-colors mode fills, gradients, blur and shadows are discarded, so the panel keeps a system-colour border, the current page is marked with CanvasText on the reserved rule it already holds in its box model, the caret and the folder/file glyphs repaint with CanvasText, the nesting guides repaint with CanvasText, and the focus ring switches to the Highlight system colour. Contrast: on every material the branch label and the leaf label measure at or above 4.5:1 against the panel and the current-page label at or above 4.5:1 against its wash; the adaptive recipe also ships a dark-scheme palette that holds the same ratios. Disabled rows fade to 45%, which WCAG exempts from contrast as inactive controls.",
  a11yEs:
    "El árbol es una región de navegación con una etiqueta accesible, y su estructura es marcado de lista real: ul/li anidados en cada nivel, cada lista con role=\"list\" para que Safari no elimine la semántica al quitar las viñetas. Deliberadamente NO es role=\"tree\": esto es navegación y no un widget de selección, así que no hay tabindex rotatorio ni búsqueda por escritura, y Tab alcanza cada botón de rama y cada enlace en el orden del código. Los destinos son anclas reales cuando reciben un href y botones reales en caso contrario, así que el navegador gobierna la activación, el clic central y abrir en pestaña nueva; cualquier glifo inicial de carpeta o archivo es aria-hidden, dejando la etiqueta como único nombre accesible. Cada etiqueta de rama es un botón que expone aria-expanded y aria-controls apuntando a su lista anidada, que permanece montada en todo momento para que esa referencia nunca quede colgando; mientras está plegada la lista es inert, así que sale del árbol de accesibilidad y del orden de tabulación y ningún usuario de teclado puede caer dentro de una rama cerrada. Enter y Espacio alternan una rama de forma nativa porque el botón es un botón real; ArrowRight abre una rama cerrada y ArrowLeft cierra una abierta; Escape pulsado en cualquier punto dentro de una rama desplegada la pliega y devuelve el foco al botón que la abrió, deteniendo la propagación para que se cierre la rama más interna y para que la tecla no descarte además un cajón o diálogo que contenga al árbol. La página actual lleva aria-current=\"page\" junto con un peso más fuerte, su propio color, un lavado Y una regla izquierda reservada de 3px — un borde transparente siempre presente en el modelo de caja que solo toma color cuando está activo — así que el estado nunca se señala solo con el color y marcarlo nunca desplaza una fila. El camino hasta ella también se lee: cada rama antecesora se despliega en el primer render (el estado se siembra durante el render inicial, así que esto vale antes de la hidratación y con JavaScript desactivado) y se marca con un peso más fuerte más su propia regla reservada en un tono atenuado que no puede confundirse con la página misma. Las filas son objetivos de ancho completo de al menos 38px de alto, 44px en md y 50px en lg. El foco muestra un anillo interior de 2px en :focus-visible usando el token de anillo del material y nunca se elimina; un gancho paralelo data-focus permite a la vista previa de la documentación mantener ese anillo visible para inspeccionarlo. El movimiento se suprime bajo prefers-reduced-motion: se descartan la transición de despliegue y el giro del cursor mientras ambos estados finales quedan intactos, así que una rama abierta sigue completamente abierta y una cerrada sigue cerrada. En forced-colors se descartan rellenos, degradados, desenfoque y sombras, así que el panel conserva un borde de color de sistema, la página actual se marca con CanvasText sobre la regla reservada que ya mantiene en su modelo de caja, el cursor y los glifos de carpeta/archivo se repintan con CanvasText, las guías de anidamiento se repintan con CanvasText, y el anillo de foco cambia al color de sistema Highlight. Contraste: en cada material la etiqueta de rama y la etiqueta de hoja miden 4,5:1 o más contra el panel y la etiqueta de la página actual 4,5:1 o más contra su lavado; la receta adaptativa además incluye una paleta para esquema oscuro que mantiene las mismas proporciones. Las filas deshabilitadas bajan al 45%, algo que WCAG exime de contraste por ser controles inactivos.",
  sourcePath: "src/registry/ui/sidebar-nav-tree.tsx",
  Preview: SidebarNavTreePreview,
};
