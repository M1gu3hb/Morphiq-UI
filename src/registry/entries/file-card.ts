import { FileCardPreview } from "@/registry/previews/file-card-preview";
import type { RegistryEntry } from "@/registry/schema";

/**
 * Registry entry for the file card component.
 *
 * One entry per file, exported as `entry`, living at
 * `src/registry/entries/<slug>.ts` where the file name IS the slug.
 * `scripts/gen-registry.mjs` discovers these files and assembles
 * `src/registry/generated.ts`.
 */
export const entry: RegistryEntry = {
  slug: "file-card",
  name: "File Card",
  nameEs: "Tarjeta de archivo",
  category: "cards",
  materials: ["clay", "glass", "skeuo", "adaptive"],
  description:
    "A file row with a type glyph or real thumbnail, the file name as a semantic heading, its type, size and modified date as text, and a hand-rolled actions menu (Download / Rename / Delete) — with a stretched-link pattern so the whole surface can open the file without trapping the menu.",
  descriptionEs:
    "Una fila de archivo con glifo de tipo o miniatura real, el nombre como encabezado semántico, su tipo, tamaño y fecha de modificación en texto, y un menú de acciones propio (Descargar / Renombrar / Eliminar) — con un patrón de enlace extendido para que toda la superficie abra el archivo sin atrapar el menú.",
  variants: [{ id: "default", label: "Default", labelEs: "Predeterminado" }],
  sizes: [
    { id: "sm", label: "Small", labelEs: "Pequeño" },
    { id: "md", label: "Medium", labelEs: "Mediano" },
    { id: "lg", label: "Large", labelEs: "Grande" },
  ],
  dependencies: {
    npm: ["class-variance-authority", "clsx", "tailwind-merge", "lucide-react"],
    internal: ["src/lib/cn.ts"],
  },
  a11y:
    "Each card is an <article> whose file name is a REAL heading; the rank is set by headingLevel rather than hardcoded, because the correct level depends on the surrounding document outline (the preview places the cards in a plain <ul>/<li> list, which the caller owns). The file type, the size and the modified date are all real text, never glyph-only: the type glyph is decorative and aria-hidden while fileType states the same thing in words, the size string is pre-formatted by the pure, locale-stable formatFileSize helper, and the date is rendered as <time dateTime={modifiedIso}> preceded by a screen-reader-only \"Modified\" prefix. Nothing calls Date.now() or new Date() during render — both the ISO value and its display string arrive as props — so a statically generated page and the hydrated one always agree. The thumbnail well is a fixed square with an aspect-square image inside it, so the row occupies identical space with or without an image and a late-loading thumbnail cannot shift the layout; a supplied thumbnail carries meaningful alt text, with alt=\"\" reserved for genuinely decorative art. When href is set the whole card becomes ONE link via the stretched-link pattern — a single <a> inside the heading whose ::after overlay covers the card, so the heading text is the link's accessible name — and the actions cluster is raised on relative z-10 so it stays independently clickable and focusable; no <a> or <button> is ever nested inside another. Linked cards draw the focus ring on :focus-within as well as :focus-visible so tabbing to the link or the trigger outlines the whole card; inert cards skip that to avoid double-ringing the trigger's own focus. The actions trigger is an icon-only <button> with a required accessible name that includes the file name (\"Actions for report.pdf\") plus aria-haspopup=\"menu\", aria-expanded and aria-controls; it grows to a 44px target on coarse pointers. The menu is a hand-rolled role=\"menu\" of role=\"menuitem\" buttons with roving tabindex: ArrowDown/ArrowUp move focus and wrap, Home/End jump to the ends, Enter and Space activate natively because the items are real buttons, Escape closes and RETURNS focus to the trigger (and stops propagating so it does not also close an enclosing dialog), Tab closes and hands focus back, and a pointerdown outside the cluster closes it. Disabled items are skipped by roving navigation. The destructive item is marked by a TEXT badge (\"Permanent\") beside its label as well as a tinted ink, so the warning survives greyscale, colour blindness and forced colours; no state anywhere is signalled by colour alone. Choosing an action announces through an aria-live=\"polite\" region that is present in the DOM before any text arrives, so the first confirmation is spoken rather than swallowed. Under prefers-reduced-motion the hover lift, press sink and menu rise are cancelled while the menu's END state — fully open and correctly placed — is kept. In forced-colors mode a system-coloured border keeps the card, the well and the menu visible once shadows, translucency and background images are discarded, the trigger falls back to ButtonFace/ButtonText, glyphs repaint to CanvasText, the active menu item takes Highlight/HighlightText and the focus ring becomes Highlight. Contrast: on every material the file name, the meta line and the menu labels measure at or above 4.5:1 against their surface, and the glass recipe carries its own tint so that holds over a white and a black backdrop alike.",
  a11yEs:
    "Cada tarjeta es un <article> cuyo nombre de archivo es un encabezado REAL; el rango se define con headingLevel en vez de fijarse, porque el nivel correcto depende del esquema del documento (la vista previa coloca las tarjetas en una lista <ul>/<li> que pertenece a quien la usa). El tipo de archivo, el tamaño y la fecha de modificación son texto real, nunca solo un glifo: el glifo de tipo es decorativo y aria-hidden mientras fileType dice lo mismo con palabras, la cadena de tamaño la produce de antemano el ayudante puro y estable formatFileSize, y la fecha se representa como <time dateTime={modifiedIso}> precedida por un prefijo \"Modified\" solo para lectores de pantalla. Nada llama a Date.now() ni new Date() durante el render — tanto el valor ISO como su texto llegan como props — así que la página generada estáticamente y la hidratada siempre coinciden. El hueco de la miniatura es un cuadrado fijo con una imagen aspect-square dentro, de modo que la fila ocupa el mismo espacio con o sin imagen y una miniatura que carga tarde no puede desplazar el diseño; una miniatura provista lleva texto alternativo significativo, y alt=\"\" queda reservado para arte puramente decorativo. Cuando se define href, toda la tarjeta se convierte en UN enlace mediante el patrón de enlace extendido — un único <a> dentro del encabezado cuyo ::after cubre la tarjeta, de modo que el texto del encabezado es el nombre accesible del enlace — y el grupo de acciones se eleva con relative z-10 para seguir siendo clicable y enfocable de forma independiente; nunca se anida un <a> o <button> dentro de otro. Las tarjetas enlazadas dibujan el anillo de foco tanto en :focus-within como en :focus-visible, así que tabular al enlace o al disparador resalta toda la tarjeta; las inertes lo omiten para no duplicar el anillo del propio disparador. El disparador de acciones es un <button> solo de icono con un nombre accesible obligatorio que incluye el nombre del archivo (\"Actions for report.pdf\"), más aria-haspopup=\"menu\", aria-expanded y aria-controls; crece a un objetivo de 44px en punteros gruesos. El menú es un role=\"menu\" propio con botones role=\"menuitem\" y tabindex móvil: ArrowDown/ArrowUp mueven el foco y dan la vuelta, Home/End saltan a los extremos, Enter y Espacio activan de forma nativa porque los elementos son botones reales, Escape cierra y DEVUELVE el foco al disparador (y detiene la propagación para no cerrar además un diálogo contenedor), Tab cierra y devuelve el foco, y un pointerdown fuera del grupo lo cierra. Los elementos deshabilitados se omiten en la navegación por flechas. La acción destructiva se marca con una etiqueta de TEXTO (\"Permanent\") junto a su rótulo además de una tinta diferenciada, así que el aviso sobrevive a escala de grises, daltonismo y colores forzados; ningún estado se señala solo con color. Elegir una acción se anuncia mediante una región aria-live=\"polite\" presente en el DOM antes de que llegue cualquier texto, para que la primera confirmación se lea y no se pierda. Con prefers-reduced-motion se cancelan el levantamiento, el hundimiento al pulsar y la subida del menú, pero se conserva el estado FINAL del menú: abierto y bien colocado. En forced-colors, un borde con color de sistema mantiene visibles la tarjeta, el hueco y el menú una vez descartadas sombras, translucidez e imágenes de fondo; el disparador recurre a ButtonFace/ButtonText, los glifos se repintan a CanvasText, el elemento activo del menú toma Highlight/HighlightText y el anillo de foco pasa a Highlight. Contraste: en cada material el nombre del archivo, la línea de metadatos y las etiquetas del menú miden 4,5:1 o más contra su superficie, y la receta de vidrio lleva su propio tinte para que eso se cumpla igual sobre fondo blanco que negro.",
  sourcePath: "src/registry/ui/file-card.tsx",
  Preview: FileCardPreview,
};
